# Core Rules + Compendium wiring audit — Aug 2 2026

Eight parallel auditors, one per subsystem, each required to quote the book, cite `file:line`, and state the player-visible consequence. Bug Hunt, Planetfall and Tactics were out of scope by request.

**152 findings**: 88 NEVER-FIRES, 21 WRONG-VALUE, 13 FABRICATED, 30 PARTIAL.

> ## Standing as of Aug 7 2026 — **0 open / 0 partial / 136 fixed / 1 corrected. THE LEDGER IS CLOSED.**
>
> The last four OPEN rows (125 Black Jobs, 136 Salvage, 241 Flee Invasion, 243
> Train) closed Aug 6; the last seven PARTIAL rows (96, 115, 119, 153, 157, 162,
> 239) and the final deferred item (87 Faction Favors) closed Aug 7. The three
> chapters listed further down as PARTIAL in prose — Dramatic Weapons, Detailed
> Injuries, Loans step 2 — were re-verified against the code and are STALE; all
> three have live callers.
>
> **Do not read this as "the rules are done."** It means every row someone wrote
> down has a call site and a test. The audit found what eight auditors reading
> eight subsystems could see; it did not walk every page of both books. The
> going-forward guard is the lints plus the per-row tests, not this number.
>
> Previous standing, for the drift record:
> ### Aug 6 2026 — **46 open / 7 partial / 91 fixed / 1 corrected**
>
> MEASURED, not asserted. Reproduce it, do not trust this line — every previous
> header here has drifted, in both directions:
>
> ```bash
> awk -F'|' 'NF>4 {v=$(NF-1);
>   if (v ~ /FIXED/) f++; else if (v ~ /CORRECTED/) c++;
>   else if (v ~ /PARTIAL/) p++; else if (v ~ /OPEN/) o++}
>   END {print "OPEN="o" PARTIAL="p" FIXED="f" CORRECTED="c}' \
>   docs/RULES_WIRING_AUDIT_2026-08.md
> ```
>
> Order the branches FIXED-first: several closed cells now quote the word "OPEN"
> inside their own history ("RE-OPENED then RE-FIXED", "STILL OPEN … CLOSED"), so
> a naive `/OPEN/` match reports three battle rows that are not open.
>
> **The battle phase is CLOSED — zero open rows in `battle-setup`,
> `battle-resolution` or `post-battle`.** That answers the standing question the
> delivery audit was opened for. Everything remaining sits outside it:
>
> | Open | Domain |
> |---:|---|
> | 16 | factions-world-compendium |
> | 10 | missions-elites-zones |
> | 13 | economy-trade-equipment |
> | 4 | patrons-rivals-quests |
> | 3 | turn-upkeep-travel |
>
> 39 of 46 are Compendium content, which is also the content being SOLD (the
> Jul 27 monetization frame is $9.99 base plus per-book unlocks). A paid unlock
> that changes nothing is worse than a free-content gap.
>
> **57 is an UPPER BOUND.** Rows written before the July/August sprints describe
> a codebase that no longer exists. Of the 8 battle rows re-verified on Aug 6,
> **4 were already stale** (Slippery Ground named the wrong condition; the
> elevation checkbox was already deleted; Suppression's "still open live sites"
> list was purged Aug 2; the Deployment Conditions row was fixed twice).
> Re-verify a row against the branch before working it.

> ## ⛔ Aug 6 — this ledger cannot see the defect that closed four chapters
>
> A separate battle-phase **delivery** audit (`0f10cbfcd`, sweeps W1-W10 in
> `review-the-core-rules-jazzy-harbor.md`) found four Compendium chapters
> unreachable in campaign play. **None of them would ever appear as a row here**,
> because this ledger's unit of analysis is *rule → producer → consumer*, and in
> every case the producer AND the consumer existed and were correct. What was
> missing was the CALL — an early return above four setup functions, a container
> with no opener, a correct function nobody invoked.
>
> Two rows below were touched as a result, and one of them is the cautionary tale:
> the **Deployment Conditions panel** row was marked `FIXED bf3f797c3` and the
> panel still rendered blank in every campaign battle, because the fix wired a call
> that ran before the panel existed. **A `FIXED` cell in this table is a claim
> about a call site, not about what the player sees.** Verify at the surface.
>
> Also corrected: the "battle-setup: 8 open" domain standing was not reproducible
> (filtering live OPEN battle-* rows yields 5, two of which were already fixed).
> Re-count, per the note directly below — it has now drifted three times.

**Status Aug 3 (late), HISTORICAL: 64 open / 71 fixed / 7 partial-or-blocked (+1 CORRECTED) of 143.** Superseded — the ledger closed Aug 7 at 0 open / 0 partial / 136 fixed / 1 corrected. Kept for the drift record.
Counts are MEASURED, not maintained by hand — the header has drifted twice, in both
directions. Re-count before trusting it; the command is in
`docs/RULES_WIRING_CLOSEOUT_PLAN.md` §0, which is also the route through what is left.
Note a status cell can read `OPEN — VERDICT CONFIRMED …`, so matching the literal
`| OPEN |` undercounts.

`NEVER-FIRES` = implemented, often with book-exact data, but no code path reaches it.
`FABRICATED` = not in either book; project policy is removal.

Status column is for tracking fixes: OPEN / FIXED (commit) / BLOCKED (reason) / CUT (needs user approval).

## NEVER-FIRES (88)

| Domain | Rule | Fix size | Player-visible effect | Status |
|---|---|---|---|---|
| factions-world-compendium | Faction generation — Compendium p.110 "Generating Factions" | medium | A brand-new Standard campaign has ZERO factions for its entire life. With "Expanded Factions" ticked in the setup wizard the player sees nothing: no faction list, no faction jobs, no Loya... | FIXED (Aug 6) — **VERDICT CONFIRMED, and it was the dead-chapter shape: complete data, correct readers, correct gate, no caller.** `active_factions` was empty for a campaign's entire life because nothing ever created a Faction. Everything downstream (jobs p.111, Loyalty/Favors p.112, Activities p.113, Events pp.114-115, Destruction p.115) is written, wired and correctly DLC-gated, and could never fire. The per-world LIFECYCLE was even built — `TravelPhase` stores the departing world's factions, clears state on arrival, restores them on a return visit — it simply had nothing to store. `initialize()` was NOT the missing call: it has no caller and no `_ready()`, and TravelPhase sets `_initialized = true` directly to skip it, rightly, because the default it ran generated 8-16 factions across eight hardcoded "categories" (government/corporate/military/pirate/alien) that appear in no rulebook. New `FactionSystem.generate_world_factions()` implements p.110 verbatim ("When creating a new world, generate 1D3+1 Factions as well"), called from TravelPhase on arrival — placed after both the cleanup and the return-visit restore, and self-skipping when factions already exist, so ONE call is correct for a first visit and a return visit alike — and from `CampaignFinalizationService` for the starting world, which is the only world TravelPhase never sees. 10 cases in `tests/unit/test_faction_generation.gd`. |
| factions-world-compendium | Faction Jobs — Compendium p.111 | medium | The captain's free, per-turn Faction job check never happens. No Faction job ever appears in the World Phase job-offer list, so the whole faction-employment loop (and the Loyalty it feeds... | FIXED (Aug 6) — **the FOURTH rule the same dead file has held hostage.** The only caller of `FactionSystem.get_faction_mission_opportunities()` was `WorldPhase.gd:939`, the zero-instantiation file whose other three victims (`record_invaded_planet()`, `repair_hull()`, the fuel-credits consumer) are already recorded in CLAUDE.md; `JobOfferComponent._generate_compendium_missions()` names them in its own docblock. The D6 check, the mission generator and the post-battle Loyalty payoff in `RivalPatronResolver` were all correct and all unreachable. Now generated on the LIVE job-offer path. Two book corrections went in with it: p.111 says "SELECT a Faction you would like to work for and roll 1D6" — **one check per turn against one faction**, where the old code looped every faction and compounded the odds (four factions at Influence 3 turns a 50% chance of a job into 94%); and "Faction jobs are treated as a Patron job, but do not roll for Danger Pay or Benefits", now stamped as `skip_danger_pay` / `skip_benefits` with `mission_source = "faction"`. The book gives the SELECTION to the player; the highest-Influence faction stands in until a picker exists, via a `preferred_faction_id` hook. |
| factions-world-compendium | Affiliated Patron jobs — Compendium p.112 | small | No Patron job is ever flagged as faction-affiliated. `is_affiliated` is permanently false at RivalPatronResolver.gd:93, so the harder "a roll of a 6 earns +1 Loyalty" branch (FactionSyste... | **FIXED** - `check_affiliated_patron()` had ZERO callers. Now rolled at `JobOfferComponent._enhance_job_with_compendium()`, the chokepoint BOTH job builders pass through. p.112 triggers on "a job from a NEW Patron", so the answer is remembered per patron identity in `progress_data["patron_faction_affiliation"]` - re-rolling every offer would let a contact drift in and out of a Faction and turn the book's flat 1/3 into 1-(2/3)^n for a frequently-seen Patron. An empty string is stored for "not affiliated" so no-roll and rolled-1-4 stay distinguishable. `is_affiliated_patron_job` now also crosses BattleResultNormalizer, or it would die at the chokepoint. |
| factions-world-compendium | Gaining Loyalty — Compendium p.112 | small | Winning a faction mission grants 0 Loyalty. Loyalty with every faction is permanently 0, which in turn makes Faction Favors (D6 <= Loyalty), the "Befriending the leadership" story point, ... | **FIXED - and this was the ROOT of the whole cluster.** Not a FactionSystem bug at all: `WorldPhaseController`'s mission_dict copies ~20 named keys off the accepted job and **`faction_id` was not one of them**, so the faction's identity died at the hand-off (the same omission that killed `patron_id` until Aug 1). `RivalPatronResolver:245` reads `battle_result["faction_id"]` to run the p.112 Loyalty roll, found "" every time, and Loyalty was **permanently 0 in every campaign ever played** - which is why rows 87, 89, 220 and half of 90 also read as dead. One missing key, six inert rules. |
| factions-world-compendium | The Benefits of Loyalty (Faction Favors) — Compendium p.112 | large | The captain can never call in a faction favor. All six book favors — cancelling a Loan enforcement roll, Credits equal to the die roll, a free Patron/Salvage job, a free trial crew member... | **UNBLOCKED, and the remaining gap is stated.** `attempt_faction_favor()` is correct and complete (D6 <= Loyalty, reduce Loyalty by the die roll, return the six book favors) and was unreachable for two reasons: Loyalty could never exceed 0 (row 86), and it has no caller. The first is fixed. The second is deliberate for now: p.112 says calling in a favor "requires a crew task, and can only be done by your captain" - a captain-only World-Phase crew task with a six-option picker, which is UI the crew-task component has no slot for. Reported, not silently skipped: the mechanism works and is callable; what is missing is the task entry point.  **CLOSED Aug 7.** The task now exists: `call_in_a_favor` in `data/crew_tasks.json` (captain-only, once per campaign turn, max_crew 1, gated on EXPANDED_FACTIONS), dispatched through a new `faction_favor` resolution type to `attempt_faction_favor()` — the caller it never had. The captain gate is enforced at the RESOLVER as well as at assignment. The picker opens AFTER the roll, because p.112 says "you can choose the favor after rolling" and Monetary help's payout IS that die. All six favors land in the new `src/core/systems/FactionFavorService.gd`; Contact network is split into two picker rows because the book gives it an either/or ("you may EITHER roll up a Patron ... OR opt to take a Salvage job"). Both Contact network branches and Provide cover bank entitlements through machinery that already exists — `patron_offers_owed`, the p.137 salvage generator, and the ONE p.85 rival-suppression chokepoint that now carries four rules (Story Events, Black Jobs, Private Transport, Provide cover). Pulling strings prefers removing an eligible Rival and banks the loan-enforcement cancellation when none exists, so the favor is never spent for nothing. 16 cases in `tests/unit/test_faction_favors.gd`, detection-proven. |
| factions-world-compendium | Starting Loyalty on the home world — Compendium p.112 | small | A crew that starts the campaign on the faction world begins at Loyalty 0 with everyone instead of Loyalty 1 with a chosen faction — losing the one guaranteed early Faction Favor attempt t... | FIXED (Aug 6) — p.111 verbatim: "If you began the campaign on this world, you may choose ONE Faction to start at Loyalty 1. The rest begin at Loyalty 0. If you are off-worlders, all Loyalties begin at 0." Applied in `generate_world_factions(force, is_home_world)`, which `CampaignFinalizationService` calls with `is_home_world = true` — every world reached by travel is one the crew are off-worlders on, so the flag is false there and no Loyalty is granted. Same selection caveat as the job check: the book lets the player choose, the highest-Influence faction stands in, and the count (one) and value (1) are the book's. |
| factions-world-compendium | Faction Activity table effects — Compendium p.113 (Office party / Plans within plans / Day to day operations) | medium | Three of the eleven activity rows (rolls 76-00, 25% of the D100 table) produce literally nothing: an Office party pays 0 credits regardless of Loyalty, Plans within plans offers no Quest,... | **FIXED - all three, and each was dead for its own reason.** *Office party* (76-80) asked every crew CHARACTER for `get_faction_loyalty()`, a method Character does not have - the only definition repo-wide is on FactionSystem - so `has_method` was permanently false and it paid 0 credits; Loyalty is a crew-vs-faction score on `active_factions[fid]["loyalty"]` and is now read from there. *Plans within plans* (81-90) and *Day to day operations* (91-00) both called `_game_state.add_mission_opportunity()`, a method with **ZERO definitions repo-wide**, AND passed `faction["name"]` to `generate_faction_mission()`, which keys on the id - two independent reasons each produced nothing. The book also distinguishes them (a Quest vs a job NEXT turn) and the code treated both as an immediate mission; now Plans grants a Quest Rumor through `GameStateManager.add_quest_rumor()` and Day to day sets `offers_job_next_turn`, which `get_faction_mission_opportunities()` consumes. |
| factions-world-compendium | Faction Destruction — Compendium p.115 | medium | Factions are immortal. A faction ground down by repeated Struggles and Undermines sits at Power 1 / Influence 1 forever and keeps acting, so the whole rise-and-fall arc of the faction sub... | **FIXED - TWO independent causes, and fixing either alone changes nothing.** (1) `_decrease_highest_stat()` clamped with `max(1, ...)`, so no stat could ever reach 0. (2) `_check_faction_destruction()` fires on `<= 0` and had **ZERO callers**. Factions were immortal: ground down by repeated Struggles they sat at Power 1 / Influence 1 forever and kept acting. The clamp is gone and the check now runs inside `_decrease_highest_stat` itself - the single chokepoint all SEVEN decrement sites pass through, rather than seven guards and seven chances to miss one. Also added the p.115 consequence half, which never existed: the vengeance Rival **rolled on the real Hired Muscle table** (16 D100-weighted rows in enemy_types.json - the book names the table, so it is rolled rather than named) gated on 4+ Loyalty to the winner, and the +2 Loyalty for having taken a job for the winner. The p.114 invasion-flee path keeps its own "-1 ... to a minimum of 1 each" floor, which is a different rule with a floor the book actually states. |
| factions-world-compendium | Invasion!? — Check for Factions fleeing — Compendium p.114 (turn sequence Step 1.2) | medium | When a world is invaded, nothing happens to its factions: none flee with -1/-1, none are destroyed, and none of the Power 5+ defenders contribute their +1 to Galactic War Progress — so th... | **FIXED** - `process_invasion()` implements every p.114 clause correctly (D6 vs Power for Power<=4, flee at -1/-1 floored at 1, destroyed otherwise, Power 5+ stay and fight) and had **ZERO callers**, so an invaded world's factions were untouched. Wired at `UpkeepPhaseComponent._record_invaded_world()`, the one place in the codebase that knows a world just became invaded. The "+1 to the Galactic War Progress table" per Power-5+ defender is written to the tracked planet's own `war_modifier` - the field `record_invaded_planet()` seeds at 0 and `GalacticWarProcessor` already reads (:39) and adds to the p.126 2D6 roll (:53). A new `progress_data` key would have been a second source of truth needing a second consumer, which is how half the rows in this audit came to exist. |
| factions-world-compendium | Fringe World Strife effects on Factions — Compendium p.114 | one-line | Even in a campaign where a Crackdown or Economic Collapse were rolled, factions keep acting normally and lose no Influence, and a Civil War does not send them to ground. The three cross-r... | **FIXED** - `process_strife_by_name()` had ZERO callers; now called from `PaymentProcessor._apply_strife_event()`, the only place that knows a strife row fired. Two corrections while wiring it: p.114 says a Crackdown prevents **ALL** Faction activities this turn, but the old code set a per-faction `cannot_act_next_turn` flag that suppressed only the ONE randomly chosen faction's D100 activity and left the mandatory p.113 Struggle running straight through it - it is a step-level block now. And Economic Collapse's -1 Influence was floored at 1, so it could never finish off a faction; it routes through the destruction check like every other decrement. Civil War is held until `end_civil_war()` rather than expiring on a turn tick, per "until the war is over". |
| factions-world-compendium | Fringe World Strife — arrival check — Compendium p.148 | medium | Fringe World Strife never activates in any campaign, even with the DLC owned and the option ticked in the setup wizard. No world is ever Unstable; the Instability track and all ten Chaos ... | **STALE - verified fixed** by `ed7a00135`. The arrival roll is live and reachable: `WorldPhaseController.initialize_world_phase()` (:610) -> `_check_compendium_world_strife()` (:649) -> `FringeWorldStrife.roll_arrival()` (:692), idempotent per world, with the p.148 4+ default and the "you may opt to use a 5+ roll" option read from `progress_data["fringe_strife_calmer"]`. Pinned by `test_arrival_uses_four_plus_and_the_calmer_option_uses_five_plus`, `test_an_unstable_world_begins_at_plus_one_instability`, `test_the_arrival_roll_is_idempotent_per_world` and `test_the_controller_does_the_arrival_roll_and_nothing_else` - 23/23 green. |
| factions-world-compendium | Check for Instability step placement — Compendium p.10 Updated Campaign Turn Sequence | small | Even after the two preceding findings are fixed, Instability would be checked at the wrong point in the turn — before the battle rather than after it — so 'completed a Patron job this cam... | **STALE - verified fixed** by the same commit, and the placement is now correct by construction. The accumulator runs in `PaymentProcessor.process_fringe_world_strife()`, called from PostBattlePhase **step 6 (Check for Invasion)** - after the battle, which is what p.148's "During the Invasion step of every campaign turn" requires and what makes the "-1 if you completed a Patron job this campaign turn" and "-1 if you Held the Field against a Roving Threat" modifiers knowable. Pinned by `test_the_accumulator_runs_in_the_invasion_step`. |
| factions-world-compendium | The 12 Difficulty Toggles — Compendium pp.32-34 | large | Ticking any of the 12 toggles in Settings changes nothing in play, and they do not appear in the campaign-creation wizard at all. Money is Tight does not change upkeep or the D6 credit ro... | **STALE - verified fixed, and the whole chain re-walked this session.** All 12 pp.32-34 options now have live consumers and each is pinned by a name-bearing case in `test_compendium_difficulty_toggles.gd` (20/20 green). The three breaks the row describes are all closed: the creation wizard DOES offer them (`ExpandedConfigPanel._build_difficulty_toggles_section` :576); the selection survives the coordinator whitelist, which now names the key (`CampaignCreationCoordinator` :678-680) and publishes a creation-time override (:686) so p.34 Starting in the Gutter can apply before a campaign exists; and finalization writes the SSOT `campaign.progress_data["difficulty_toggles"]` (:432-434) then clears the override. Money is Tight reaches all three of its clauses - `roll_credit_die()` at every 1D6-for-credits site, `crew_task_surcharge()` in CrewTaskComponent, `free_hull_repair_denied()` in CampaignPhaseManager - plus the upkeep scale in UpkeepPhaseComponent :239. `user://difficulty_toggles.cfg` is a documented FALLBACK consulted only when there is no campaign (Battle Simulator), never a second owner. |
| factions-world-compendium | Progressive Difficulty Options 1 and 2 — Compendium pp.30-31 | large | With Progressive Difficulty Option 1 enabled and the campaign at Turn 20, the player still faces the base rolled enemy count with zero respawns instead of +2 basic +1 specialist +1 Lieute... | PARTIAL 0eead4056 (Option 1 Strength + instructions live; Option 2 toggle unlocks pending)  **CLOSED Aug 7** — Option 2 was a block of INSTRUCTION TEXT: the p.31 milestone table was byte-faithful and its only consumer printed it, so a player was told every battle from turn 3 to "Enable Strength-Adjusted Enemies" while the toggle stayed off. Five of the eight milestones name toggles the codebase already implements at live rule sites, so `ProgressiveDifficultyTracker.option_2_toggle_unlocks()` now merges those ids into `compendium_difficulty_toggles.get_active_toggles()` — the ONE call every rule site reads, so all twelve `is_toggle_active()` consumers honour the unlock without a branch each. The elite milestones become a per-encounter D6 in `EnemyGenerator._option_2_blocks_elite()`: turn 14 on 4+, 16 on 3+, 20 always, and NO elite enemies before 14 (p.48's unconditional substitution still stands on its own for a player who simply turned Elite Enemies on) |
| economy-trade-equipment | Trade Table entries that send you to another table (Core Rules p.79-80) | small | 24 of 100 Trade Table results (rolls 1-3, 7-9, 45-48, 79-81, 82-86, 87-91) show a description dialog and award nothing at all. Every crew member sent to Trade has a ~1-in-4 chance of comi... | FIXED e2e56b985 |
| economy-trade-equipment | On-board Items (Core Rules pp.57-58) — 19 items with campaign-turn effects | large | Every on-board item in the Stash is inert. A Purifier never produces its 1 credit per campaign turn; Lucky Dice / Loaded Dice never gamble; Repair Bot and Spare Parts add no +1 to the Rep... | FIXED (uncommitted) — the data was byte-exact against pp.57-58 and `EquipmentManager.get_onboard_item_effect()` returned correct structured effects; its ONLY caller repo-wide lived in the DEAD `phases/TravelPhase.gd`. Complete data, correct reader, no caller. New SSOT `src/core/equipment/OnboardItemService.gd`, wired at every rule site: **Repair Bot / Spare Parts** (+1 each, Spare Parts consumed on a natural 1) in `_resolve_repair_task`; **Mk II Translator** as a genuine EXTRA D6 (not +1 — against a 6+ target they are not interchangeable) in `_resolve_dice_task`; **Teach-bot** rolled-and-consumed in the Train branch; **Purifier / Lucky Dice / Loaded Dice** as a once-per-turn income step in `initialize_upkeep_phase` (a Loaded 6 loses BOTH sets and demands an Injury Table roll); **Analyzer** in BOTH quest systems, computed above the branch. The remaining ten are single-use player choices behind a new `OnboardItemUseDialog` opened from the Upkeep step; three of them (**Nano-doc**, **Genetic Reconfiguration Kit**, **Colonist Ration Packs**) ARM a flag their own system consumes, because each modifies a roll that happens later — p.58's Nano-doc says outright "You must decide before rolling the dice". **Two polarity findings.** The Analyzer's single sentence covers two rolls of OPPOSITE polarity: p.85 Rumors succeeds on a LOW roll, so its +1 must move the THRESHOLD (adding it to the die would make a paid-for item a penalty), while Quest resolution succeeds on a HIGH total and takes the +1 on the ROLL. And **Fake ID / Sector Permit were inert by construction** — see row 100's Interdiction note. 34 cases in `tests/unit/test_onboard_items.gd`, four fixes detection-proven by isolated revert. |
| economy-trade-equipment | Gun Mods and Gun Sights (Core Rules p.53) | large | Looting or buying a Unity Battle Sight, Cyber-configurable Nano-Sludge, Upgrade Kit, Shock Attachment, Stabilizer, Hot Shot Pack, Assault Blade, Bipod, Beam Light, Laser Sight, Quality Si... | FIXED (uncommitted) — all 13 attachments shipped in `data/equipment_database.json` with byte-accurate p.53 effects and all 13 are rollable off the p.131 Loot Table (`gun_mods` 1-20 + `gun_sights` 21-40 of the Gear subtable, a fifth of all gear loot). NOTHING could fit one to a weapon. **The sharpest instance of the audit's core shape**: `BattleCalculations.calculate_hit_modifier()` has implemented the Bipod clause verbatim since it was written, gated on `modifiers.has_bipod` — a key with ZERO producers repo-wide. And the ONE live caller, `CharacterQuickRollPanel`, passed `var modifiers := {}`, so Heavy's -1 for moving and Snap Shot's +1 within 6" (both plain p.51 weapon traits, nothing to do with p.53) were dead at the same site. New SSOT `src/core/equipment/WeaponModService.gd` owns the fitting rules (one Mod + one Sight per weapon; Mods permanent so there is deliberately NO `remove_mod()`; Sights movable and a displaced Sight returns to the kit rather than evaporating; Non-Pistol/Pistol-only and the Hot Shot Pack's four named weapons all enforced at the service, not the UI), the derived weapon profile (Upgrade Kit / Quality Sight +2" Range, Assault Blade Melee +1 Damage, Stabilizer strips Heavy, Shock Attachment adds Stun, Laser Sight adds Snap Shot — returned as a COPY so re-rendering can never compound the +2"), and the battle inputs. Fitting UI lives in **Assign Equipment** because p.53 puts it there verbatim ("moved to a new one when equipment is being assigned during the campaign turn"). `propagate_damage_to_sight()` wired at the p.127 stash-damage writer for "If the weapon is damaged, any Sight attached also becomes damaged", and a damaged Sight grants nothing. `calculate_hit_modifier` gained a `mod_bonus` parameter for the two unconditional +1s (Nano-sludge, Unity Battle Sight); the two CONDITIONAL sights (Seeker "did not Move", Tracker "same target last round") are returned as flags and asked as checkboxes, because the app cannot know either — and the Tracker checkbox only appears when a Tracker Sight is fitted. 21 cases in `tests/unit/test_gun_mods_and_sights.gd`, four fixes detection-proven; 82-case `test_battle_calculations` still green. |
| economy-trade-equipment | Damaged loot requires Repair before use (Core Rules p.131 + Repair Your Kit p.78) | medium | Loot rolls 26-45 (20% of every loot result) hand the player two supposedly-broken items that carry no [DAMAGED] tag in the Assign Equipment screen and can be issued and used immediately a... | FIXED (uncommitted) — **a producer/consumer key mismatch, not a missing feature.** `LootTableResolver._item()` marked p.131 rows 26-45 ("Both items require Repair", a fifth of all loot) with `needs_repair`/`quality`, while every rule that ACTS on damage reads `damaged`: Repair Your Kit's stash scan, the "[DAMAGED]" suffix in Assign Equipment, the p.127 campaign-event damage picker. So broken loot was never labelled and could never be repaired. Canonicalised on `damaged` (5 readers vs 2), with a new shared predicate `EquipmentTransferService.is_item_damaged()` that also accepts the legacy `needs_repair` spelling — dropping it would silently un-break every damaged item already on disk. Repair now clears BOTH spellings plus the quality/description. **Two further findings while verifying.** (a) p.78 bullet two reads "A natural 1 always fails this roll. A failed roll means the item is beyond fixing." Taken literally ANY failure destroys the item; the code retries. Kept as the natural-1 reading and recorded as a DOCUMENTED READING at the site — the designer FAQ has no entry, and the harsher reading would be inventing a rule. (b) The **Interdiction** World Trait (p.75, "approved to stay for 1D3 campaign turns... obtain a license. Roll 2D6, requiring an 8+") had ZERO consumers, and it is the ONLY licence roll in live campaign play — which is exactly why Fake ID and Sector Permit had nothing to attach to. New `src/core/world/InterdictionRule.gd` rolls the stay on arrival, rolls the Sector Permit there ("whenever you ARRIVE"), and gates the Stay button with the 2D6 8+ roll carrying Fake ID's +1. |
| economy-trade-equipment | Merchant school — reroll one Trade roll per campaign turn (Core Rules p.125) | medium | Paying 10 XP/credits for Merchant school buys nothing: the crew member gets no reroll on the Trade Table when they Trade. The one button that does exist (in the Trading phase) rerolls a m... | **FIXED - and the root cause was bigger than this row.** Merchant school bought nothing because **the whole `has_*_training` mechanism is dead**: `AdvancementSystem._apply_training_benefits()` records each p.125 course as `character.set("has_<x>_training", true)`, a property Character does NOT declare (so a silent no-op - `Character.gd:1067` says so outright and warns "never read them"), and it is reachable only from `purchase_training()`, which has **ZERO callers**. The live grant path is `Character.add_training()` -> `acquired_training`. New SSOT reader `AdvancementSystem.member_has_training()` / `.crew_has_training()` (dual-shape, Dictionary branch FIRST because `has_method()` on a Dictionary unwinds the caller). **Collateral fix in the same family:** `RedZoneSystem.can_obtain_license()` read `has_broker_training`, so a crew member who actually BOUGHT Broker training got no Red Zone licence discount - only a lucky creation trait did. The reroll itself lands exactly where p.125 puts it: *"You may roll up ALL eligible Trade rolls BEFORE choosing what to reroll"* - after the free-trade rolls, before the event queue pays anything out. One per campaign turn ("each campaign turn", not per merchant), and the new roll REPLACES the old (*"the new roll must be accepted"*) rather than being best-of-two, which a test pins by forbidding `maxi()` in that function. Eligibility reading recorded at the site: any Trade roll this turn is a candidate, because "choosing what to reroll" presupposes a choice. |
| economy-trade-equipment | Rewards Subtable — Ship Parts / Military Ship Part discount (Core Rules p.134) | medium | Rolling 71-90 on the Rewards Subtable (20% of Rewards results, i.e. 4% of all Loot Table rolls) produces a line of text and no benefit — the promised 1D6 or 1D6+2 credit discount is never... | **FIXED.** Core Rules p.134 Rewards Subtable rows 71-85 Ship Parts (1D6) and 86-90 Military Ship Part (1D6+2): *"Discount your next ship component purchase by 1D6 credits. **Establish value now**."* `LootTableResolver` already rolled it at establish-time into `ship_component_discount` and `LootProcessor._apply_loot_reward()` threw it away - its own docblock said "has no purchase-discount system yet". Banked to `progress_data["ship_component_discounts"]` as a QUEUE, not a running total: each row discounts "your NEXT purchase", so two rewards are two vouchers spent on two purchases rather than one doubled discount, which is the literal reading (stacking them would be inventing). Spent in `ShipManager`, which IS live (reachable from CampaignDashboard:349 via SceneRouter `ship_manager`): shown in the component price via `_pending_component_discount()` so the player can see the voucher they hold, and consumed by `_consume_component_discount()` **after** the affordability check - above it, a click the crew could not pay for would burn the voucher. Pinned by an ordering test. |
| economy-trade-equipment | Loot Table resolution — single source of truth | small | None directly — dead code. It matters only as a maintenance hazard: a future fix applied here (or to the JSON it reads) would appear to work while the live path (LootTableResolver) stayed... | **FIXED** — was one dead duplicate; turned out to be FOUR live ones plus the dead one. `LootSystemConstants`'s three item helpers (feeding the test-only `FPCM_PostBattleProcessor`), `CrewTaskComponent._roll_on_subtable`, `CrewTaskEventDialog._roll_subtable` and `PostBattleSequence._roll_on_subtable` each reimplemented the p.131 procedure and each got the third roll wrong the same way. All now delegate to `LootTableResolver.roll_item_in()`. `test_loot_table_distribution.test_no_resolver_picks_from_the_item_list_itself` scans all five files for the `items[randi()` shape so the next copy-paste fails a test rather than shipping. |
| battle-setup | Number of Opponents — campaign difficulty modifiers (Easy -1 at 5+, Challenging reroll 1-2, Hardcore/Insanity +1) | small | A Hardcore campaign fights the same number of enemies as a Normal one — the +1 enemy the player chose Hardcore for never appears. A Challenging campaign never rerolls the 1s and 2s, so it... | FIXED 24c657af4 |
| battle-setup | Number of Opponents — crew in the field below standard size | medium | A crew that goes into battle two or more figures short — after casualties, Sick Bay, a Small Encounter sit-out, or the player deliberately leaving people on the ship — still faces the ful... | FIXED 24c657af4 |
| battle-setup | Determine the Objective — the final battle of a Quest is always Fight Off and adds +1 enemy | small | The climactic final battle of a Quest is generated as an ordinary fight: the objective is rolled at random off the D10 Quest table instead of being forced to Fight Off, and the enemy forc... | FIXED 2c44839f0 |
| battle-setup | Seizing the Initiative — opponent-type modifiers | medium | Fighting Punks, Brat Gang, Roid-gangers, Security Bots, Abandoned, Converted Acquisition or Haywire Robots, the player silently loses the +1 they are owed and needs 10+ instead of 9+ on 2... | FIXED 2d052d2de (Careless/Alert as modifiers; Prediction/Unpredictable as absolutes on the enemy record) |
| battle-setup | Choose Your Battle — Continue a Quest is a selectable job option | large | A player who resolves Rumors into a Quest (ResolveRumorsComponent.gd:168-190 does store one) has no way to fight it. There is no 'Continue a Quest' battle option, and every quest-flavoure... | FIXED 2c44839f0 |
| battle-setup | Unique Individuals — a Guardian-AI Unique must be attached to a figure in the enemy force | small | Roughly a third of the Unique Individual table has Guardian AI (Enemy Bruiser 1-6, Mutant Bruiser 57-61, Gene Dog 86-91, Sand Runner 92-96, Mk II Security Bot 97-100 — 27 of 100 results).... | FIXED (Aug 6) — the count is **26** of 100, not 27 (6+5+6+5+4). `EnemyGenerator._attach_guardian_uniques()` now picks the target the moment both the finished roster and the Unique exist, exactly as p.94 states it: "This will always be a Lieutenant, if one is present; otherwise just pick a random non-Specialist figure" — always the Lieutenant when one exists, no weighted pick. If every remaining figure is a Specialist it still attaches to one, because the book makes the attachment mandatory ("it MUST be attached to a figure in the enemy force") and only expresses the non-Specialist preference for the random case. The pairing is written both ways (`guardian_attached_to` on the Unique, `guardian_escorted_by` on the target), carried onto `TacticalUnit`, and printed on the ENEMY_ACTIONS card together with the p.43 routine it governs: "must always remain within 3\" of that figure ... will move at the same pace and attack the same targets using the same methods." Without the target named the player could not run the figure at all. |
| battle-setup | Defend objective — force the enemy AI to Aggressive and add +1 enemy | small | On a Quest Defend objective (D10 5-6, so 20% of Quest missions) the player fights one fewer enemy than the rules require, and a Cautious/Defensive/Tactical force keeps its cautious AI — s... | FIXED 3aa8d0b88 |
| battle-setup | Battle Events (optional) — Environmental hazard | small | Low severity for a companion app — the event's text does reach the player, who resolves the rolls on the table. The concrete loss is that the app offers a dice-rolling assist for every ot... | FIXED (Aug 6) — and a **producer/consumer key mismatch** was hiding underneath. The registry writes `hazard_radius` / `crew_save` / `enemy_save` / `damage` / `ignore_armor` / `one_time_only`; `_apply_environmental_event()` read `radius` / `save_difficulty` / `damage_bonus` / `effect_type` / `permanent` — five keys NO producer writes, so every field silently took its default and the enemy's easier 4+ target, the armor-ignoring clause and "the feature is safe afterwards" never left the registry. Now read through, with `save_target_from()` parsing the target out of the registry's string form. The assist itself is `TacticalBattleUI._battle_event_roll_instructions()`: p.117 55-60 is the one Battle Events row whose target number DIFFERS PER FIGURE ("1D6+Savvy and achieve a 5+"), so a generic "make a save" line is not an assist — each crew member is listed with the number their die actually has to show (`crew_target - Savvy`, floored at 1, and stated honestly as "cannot pass — automatic hit" above 6), plus the enemies' flat 4+ and the Damage +1 Hit that ignores Armor Saving Throws. |
| battle-setup | Determine Deployment Conditions — present the rolled condition in the battle UI | small | The dedicated Deployment Conditions panel in the tactical battle screen renders blank/placeholder for every battle, and its Acknowledge/Details buttons act on nothing. The player who goes... | FIXED bf3f797c3 — the panel had roll_condition() and display_condition() and NOTHING called either. The condition was never missing; CampaignTurnController stamps it and it simply never reached the one screen built to show it. Rehydrated by id, not re-rolled. **RE-OPENED then RE-FIXED Aug 6 (`0f10cbfcd`): `bf3f797c3` was necessary but NOT sufficient.** `_populate_deployment_conditions()` was called from a point that ran BEFORE the panel it fills was constructed, and on the campaign path an early return in `initialize_battle` meant the panel was never constructed at all — so the panel still rendered blank in every campaign battle after the 'fix'. Moved into `_on_tier_selected`, after the panel exists. **A FIXED row is a claim about a call site, not about what the player sees.** |
| patrons-rivals-quests | Quest finale: "Next time you pursue a Quest mission, it will be the finale… add +1 to the number of opponents… roll the die twice, pick the better score, and add +1… Crew completed the final stage of a Quest +1 XP" (Core Rules pp.120, 123) | medium | A Quest can never end. Reaching 7+ on the p.120 progress roll writes a flag the app never reads again: the finale battle never gets its extra enemy, never pays the double-roll +1 (so a fi... | FIXED 2c44839f0 |
| patrons-rivals-quests | Quest missions exist as a battle type — "Continue a Quest / If you have an active Quest" (Core Rules p.85 Select Your Job) | medium | After the Resolve Rumors step hands the player a Quest, nothing in the app ever lets them go on it. The Quest objective table (p.89: Move Through/Search/Defend/Acquire/Fight Off), the Que... | FIXED 2c44839f0 |
| patrons-rivals-quests | Benefits, Hazards and Conditions subtables — all 30 entries (Core Rules pp.83-84) | large | Every Patron job plays identically. A 'Dangerous Job' hazard fields the same number of enemies as a 'Security Team' benefit; a 'Small Squad' job still lets you deploy 6; a 'Vengeful' patr... | PARTIAL fab705684+cfbd4f91f (19 of 21 rows applied; Private Transport needs RivalEncounterCheck — another session's file — and Busy has an accessor with no caller)  **CLOSED Aug 7** — the last two of 21. **Private Transport** (p.84, and 3 of the 21 Benefits rows land on it, making it the commonest Benefit in the subtable): `blocks_rival_tracking()` was correct and its only possible consumer was the p.85 check in a DIFFERENT file, which never called it — now wired into `CampaignTurnController._story_rival_suppression_reason()`, the single suppression chokepoint, beside the Black Job clause. **Busy** (p.84): `offers_new_job_on_success()` had zero callers; `RivalPatronResolver` now banks the patron id into `progress_data["patron_followup_offers"]` and `JobOfferComponent._consume_patron_followups()` spends it next turn. Deliberately NOT folded into `patron_offers_owed` — that draws a RANDOM existing Patron and Busy names the specific employer who was pleased with the work |
| patrons-rivals-quests | Danger Pay 10+: "+3 credits and roll twice, picking the higher die when rolling for mission pay after the battle" (Core Rules p.83) | small | A Corporation job that rolls 10+ on Danger Pay advertises "roll twice for mission pay" in the job details, then pays a single 1D6. Average mission pay on those jobs is ~3.5 instead of ~4.... | FIXED fab705684 |
| patrons-rivals-quests | "When you travel to a new planet, all Patrons become unavailable, unless they are Persistent" (Core Rules p.119 Step 2) | small | Patrons accumulate forever and follow you across the galaxy. Each one adds +1 to the p.77 Find a Patron roll (CrewTaskComponent.gd:506-508) and generates 1-3 more job offers every single ... | FIXED 592a67212 (the travel purge existed; nothing ever SET is_persistent, so the Benefit could spare nobody) |
| patrons-rivals-quests | Time Frame Table — "the number of campaign turns within which you must finish the job. If the job isn't done when the time runs out, it counts as a failure" (Core Rules p.83); "a Patron job will fail if the time to complete it has expired" (p.85) | large | 'This campaign turn' means nothing — the player can decline every job and the same Patron re-offers fresh work next turn with no penalty. The Secretive Group's +1 Time Frame bonus (its on... | FIXED fab705684 (offers persist on the campaign and lapse; Vengeful fires on a lapse) |
| patrons-rivals-quests | Spending credits for +1 on crew tasks — Find a Patron (p.77), Track (p.78), Repair Your Kit (p.78), extra Trade rolls (p.78) | medium | Credits cannot buy anything in the World step. A player sitting on 20 credits cannot pay to find a Patron, cannot pay to track down the Rival hunting them, and cannot buy spare parts to s... | PARTIAL 8210675ca (+1-per-credit done; 3-credit extra Trade roll open)  **CLOSED Aug 7** — see row 157; same item, both closed together |
| patrons-rivals-quests | Interested Parties: "During Quest missions, when rolling for the number of opponents, reroll any die scoring 1 once" (Core Rules p.99) | small | Quest battles against Interested Parties are easier than the book intends — a die showing 1 stands, so the enemy force can be as small as 1-2 figures where the book guarantees a reroll. M... | FIXED 24c657af4 |
| patrons-rivals-quests | Vigilantes — "Persistent: If encountered as Rivals, all rolls to remove them from Rival status are at -1" (Core Rules p.99) | small | Vigilante Rivals are exactly as easy to shake off as any other (50% on a 4+ instead of the intended 33% on an effective 5+). The one enemy in the book designed to be a long-term nuisance ... | FIXED 5eeee2c39 |
| patrons-rivals-quests | Renegade Soldiers — "Grudge: If encountered as Rivals, they bring one additional figure" (Core Rules p.99) | small | A Renegade Soldier Rival brings the same force as any other Rival — one fewer enemy than the book specifies on every Rival battle against them, for the whole campaign. | FIXED 5eeee2c39 |
| patrons-rivals-quests | Bounty Hunters — "Intrigue: Roll 2D6 and add +1 if you killed a Lieutenant and/or Unique Individual. On a 9+, you obtain a Quest Rumor" (Core Rules p.99) | small | Beating Bounty Hunters — including killing their Lieutenant — never yields the Quest Rumor the book promises. Since Quest Rumors are the only way to start a Quest (p.85), one of the game'... | FIXED 5eeee2c39 |
| patrons-rivals-quests | Affiliated patron jobs — `battle_result["is_affiliated_patron_job"]` (Compendium p.114 faction loyalty on a patron job) | small | Faction loyalty from patron jobs always uses the non-affiliated (lower) branch, so factions never build loyalty faster from jobs that should count double. Low impact for a standard non-DL... | **STALE - fixed this session** with the faction cluster. The chain is complete end to end: `JobOfferComponent._stamp_patron_affiliation()` (:1746) decides it at the one chokepoint both job builders pass through and remembers the answer per Patron in `progress_data["patron_faction_affiliation"]`; `WorldPhaseController` (:1454) forwards it onto mission_data; `BattleResultNormalizer` (:84) carries it across the post-battle boundary; `RivalPatronResolver` (:253) consumes it for the p.112 'a roll of a 6 earns +1 Loyalty' branch. Before this it was a consumer reading a key NO producer wrote, so every affiliated job silently used the easier direct-job odds. |
| missions-elites-zones | Black Jobs — the mission itself (access, D10 'Your Day in Hell' objective, Roving Threats opposition, all rewards) | medium | Player meets the 10-Red-Zone-turn requirement, clicks "Accept Black Zone Mission", pays the travel cost — and then fights an ordinary randomly-generated battle. No Roving Threats opponent... | FIXED (Aug 6, final sweep) — the rewards, the Roving Threats source and the D10 roll were already wired; **the MISSION was not**. `get_setup_rules()` / `get_opposition_rules()` / `get_active_passive_rules()` / `get_ending_rules()` were all byte-faithful to pp.150-151 and had ZERO callers between them. Now: `BattleSetupRules._apply_black_zone()` (applied LAST so p.151's absolutes win) sets `early_leave_is_casualty` ("any crew member that flees ... automatically becomes a casualty") and `hold_rounds` from the D10 row; `CampaignTurnController` suppresses the Notable Sight + Deployment Condition and adds the Seize the Initiative +1; `_stamp_black_zone_objective()` makes the "Your Day in Hell" row the tracked objective, stamped BEFORE the p.89 roll so its `has("objective_details")` guard leaves it alone; `EnemyGenerator` fields **4 teams of 4 = 16** (replacing the rolled count, like the Red Job base of 7) with one Specialist per team; `FPCM_BattleFlowGuide.build_black_job_round_prompts()` delivers the per-round reinforcement + Passive-activation rolls and the p.151 "evac'ed out at the end of the FOLLOWING round" clause. The prep card now READS `get_opposition_rules()` instead of hardcoding "4 teams of 4" — that literal was the only place those numbers existed anywhere in the app |
| missions-elites-zones | Mission Selection — Stealth / Street Fight / Salvage battle types (and therefore the whole Stealth, Street Fight and Salvage chapters) | medium | Buying the Fixer's Guidebook DLC and enabling Stealth Missions / Street Fights / Salvage Jobs changes nothing. Every battle in every campaign is a Conventional battle. The player never se... | FIXED 6c8e7b513 — every layer already existed and agreed on the key; the only caller of all three generators was the zero-instantiation phases/WorldPhase.gd. Also needed `type` added to the WorldPhaseController hand-off, which drops ~20 keys and never carried it. Unblocks L69/L70/L71/L141. |
| missions-elites-zones | Elite-Level Enemies — Elite Composition, Specialist/Lieutenant/Captain upgrades, Unique Individual 7+, Leadership Panic table, Better Loot, Patron Pay, Quest Rewards, Brutal Fight, Elite-level Rivals | large | The 'Elite Enemies' DLC toggle is a placebo switch. Enemy forces are always built from the Core Rules p.93 thresholds (1-2 none / 3-6 one Specialist / 7+ two). No Captain ever appears, Sp... | **FIXED - five of six entry points had ZERO callers.** `compendium_elite_enemies.gd` implemented the whole chapter correctly and only `roll_enemy_in_category()` was ever called, so the option swapped the enemy NAMES and nothing else: forces were still built on the Core Rules p.93 thresholds. Now wired, all verified against Compendium pp.48-49 page text this session. (1) `enforce_minimum_size()` - p.49 'if the size would be less than 4 figures, increase it to 4'. (2) `get_composition()` REPLACES the p.93 thresholds with the book's four-row table (4: 3/1/-/-, 5: 2/2/1/-, 6: 3/2/1/-, 7+: 3+/2/1/1) - the **Captain rank does not exist in the Core Rules at all**, so before this it could never appear at any force size. (3) The p.49 unit upgrades: Specialist CS floor +1; Lieutenant and Captain gain Fearless and the Armor Saving Throw clause ('increase by +1, otherwise add 5+'), applied by REPLACING the existing rule string because `BattleResolver._extract_enemy_saving_throw()` returns on the FIRST match and an appended upgrade would be invisible; Captain carries a Ripper Sword, CS +1 and a Toughness floor of 5. (4) `unique_individual_threshold()` - 7+ instead of 9+, automatic when the crew outnumbers the enemy; combined with `mini()` so it cannot UNDO Compendium p.33 Better Leadership, and the Core Rules p.93 Invasion prohibition still stands over it. (5) `modified_panic_range()` - the p.49 Leadership table, highest rank present, applied to the rank and file before the Unique Individual is appended (a Unique is Fearless in its own right, p.105). (6) `rival_follows_to_new_world()` - Elite Rivals now follow on 4+ instead of the p.72 baseline 5+; tagged at birth in `_append_rival` and carried by the normalizer as `enemy_is_elite`, because the travel roll happens turns later when nothing else can tell where the Rival came from. Elite status is read from the TEMPLATE (`elite` stamped by the elite table roll), not from the DLC flag - so a category with no elite table correctly keeps the core composition. Red Jobs keep p.150's own three-special-figure rule. 16 new cases in `tests/unit/test_elite_level_enemies.gd`, detection-proven by two isolated reverts (Captain row -> 8 assertion failures; elite follow target -> the behavioural travel test); 86/86 across four enemy suites. |
| missions-elites-zones | Red Job — Threat Condition (D6 rolled before every Red Zone mission) | small | Every Red Zone battle is fought with no Threat Condition. The player is told one exists but is never given the roll or the result, so a Red Job never gets Pitch Black 6" visibility, never... | FIXED (Aug 6) — **a producer with no caller and a consumer with no producer, on opposite ends of the same key.** `RedZoneSystem.roll_threat_condition()` was written and correct, and its ONLY mention anywhere in the tree was the usage example in its own docblock — zero callers. Meanwhile `PostBattleCompletion.gd:149` reads `red_zone_threat` off the battle result to journal it, so the consumer was live and waiting. Now rolled once when the Red Job is accepted (`WorldPhaseController._stamp_red_zone_threat()`, at both `is_red_zone` sites), which is where p.149 puts it: "applied to the mission, regardless of its type". The three mechanical results reach real consumers in `EnemyGenerator` — roll 2 Elite Opposition and roll 5 Armored Opponents as FLOORS (`maxi`, so the book's "if not applicable ... simply ignored" falls out for free), roll 4 Heavy Opposition as +2 enemy, roll 6 Enemy Captain as an appended Lieutenant with Combat Skill and Toughness SET (the book says "regardless of the normal profile used"). **Documented reading on roll 4**: p.150 says "No other modifiers are applied up or down", but that governs the number-DETERMINATION step it sits in, and Threat Conditions occur ONLY on Red Jobs — if p.150 suppressed them, roll 4 would be permanently dead text on the only mission type that can produce it. 8 cases in `tests/unit/test_red_zone_threat_condition.gd`. |
| missions-elites-zones | Red Job — Time Constraint (D6 at end of Round 6) | medium | A Red Zone battle runs to its natural end with no round-6 event. The player never gets the reinforcement waves, **[Aug 6: VERDICT CONFIRMED, still open. Same shape as the Threat Condition row above — `RedZoneSystem.roll_time_constraint()` exists, is correct, and has ZERO callers; the `roll_time_constraint()` that IS called (`JobOfferComponent.gd:1697`) is a DIFFERENT function on `compendium_missions_expanded.gd`, for Expanded Missions, not Red Jobs. Do not mistake one for the other. Unlike the Threat Condition this cannot be stamped at mission acceptance — p.149 says "at the end of Round 6", so it needs a round-loop hook, which is why it was left for a focused pass.]** never gets the escalating Count-down clock, and never gets the 'Evac now!' ... | **FIXED - the round-loop hook the row asked for.** `RedZoneSystem.roll_time_constraint()` was correct with ZERO callers, so a Red Zone battle ran to its natural end: no reinforcement waves, no escalating Count Down clock, no 'Evac now!'. **The trap the row warned about held** - the only live `roll_time_constraint()` is a DIFFERENT function on `compendium_missions_expanded.gd`, for Expanded Missions, and grepping the name finds it and proves nothing; a test now pins that the battle UI calls `RedZoneSystemRef`'s. Resolved in `TacticalBattleUI._show_end_phase_ui()` via new `_resolve_red_job_time_constraint()`, BEFORE the prompts are built so the banner reports the outcome rather than asking for a roll the app already made (the house pattern: DiceManager rolls, the companion tells the player what to do with their figures). New `BattleFlowGuide.build_red_job_round_prompts()` warns the clock is coming before Round 6, restates the rolled instruction EVERY round afterwards (a reinforcement wave the player must place is not a one-time notice), and escalates the Count Down range 1 -> 1-2 -> 1-3 per the book. **The half with campaign consequences:** rows 4 and 5 both say *"you do not Hold the Field"*, which gates the p.119 Rival removal roll, p.120 Battlefield Finds and p.121 Loot - resolved through ONE `_apply_red_job_hold_denial()` chokepoint applied at all THREE `held_field` producers (results-form prefill, played, map auto-resolve), with a test that counts the call sites because a guard applied to N-1 of N is the most common defect shape in this codebase (detection-proven by unguarding one). Also completed three p.149 instruction clauses the JSON had dropped - "Pick the entry point closest to a crew figure" on rolls 2 and 3, and the whole Hold the Field consequence on roll 4. In a tabletop companion the instruction text IS the product, so a dropped clause is a dropped rule. |
| missions-elites-zones | Red Job Improved Rewards — additional Loot Table roll on a Win | one-line | Winning a Red Zone battle gives exactly 1 Loot roll, the same as any ordinary win. The advertised Red Zone reward the UI promises at MissionPrepComponent.gd:372-374 ("extra Loot roll on W... | FIXED (Aug 6) — p.150 verbatim: "If you Win, you may make an additional roll on the Loot Table after the battle. You receive this roll AS LONG AS YOU WIN, even if you subsequently withdrew from the battlefield." Added in `LootProcessor` as an ADDITIVE roll alongside the Story Track bonus, keyed on the win rather than on holding the field — that second sentence exists precisely to separate the two. |
| missions-elites-zones | Black Job victory reward — 3 rolls on the Loot Table | one-line | Even if the Black Zone mission gap (finding 1) is fixed, a Black Zone victory yields 1 Loot roll instead of 3 — the player loses two of the three promised Loot Table items, while the jour... | FIXED (Aug 6) — `BlackZoneSystem.calculate_rewards()` has always returned `loot_rolls: 3` and NOTHING read it. `PaymentProcessor` consumes the credits, the ship-loan payoff, the Rival clear and the 2 Patrons from that same dict and stops short of the loot. Now an absolute count in `LootProcessor`, in the same branch shape as the p.120 Quest finale. |
| missions-elites-zones | Black Job advantage — three free rolls on the Weapon Table | small | Accepting a Black Job grants zero free weapons. The player goes into the game's hardest fight with exactly the gear they already owned, losing three free Weapon Table items the book hands... | FIXED (Aug 6) — p.150: "You may roll three times on the Weapon Table in the 'Loot' chapter, and CLAIM THE ITEMS IMMEDIATELY. You can keep them afterwards, too." `get_turn_advantages()` has said `free_weapon_rolls: 3` since the file was written and had ZERO callers, so the crew walked into a near-suicide mission with nothing extra. Granted at acceptance (not post-battle, and not contingent on winning) in `UpkeepPhaseComponent._grant_black_job_weapon_rolls()`, routed through `EquipmentTransferService.add_loot_to_stash()` because the stash has one mutation API. The roll goes through a new shared `StartingEquipmentGenerator.roll_on_weapon_table()` rather than a third hand-rolled copy of the Military Weapon Table — gear_database.json stays the SSOT. |
| missions-elites-zones | Black Job advantage — immunity from Rival interference | small | On a Black Zone campaign turn a Rival can still roll to track the crew down and hijack the turn with a Rival battle, which the book explicitly forbids — the player loses their Black Job (... | FIXED (Aug 6) — p.150: "Black Jobs are never subject to interference from Rivals (no starport scum is crazy enough to attack an armed Unity drop ship)." Stated as an absolute for the whole campaign turn, so it went on the EXISTING suppression chokepoint (`CampaignTurnController._story_rival_suppression_reason()`, which already carries the Appendix V story-event suppressions) rather than as a second guard in the encounter path — two places suppressing the same check is how the p.120 Rival payment rule ended up duplicated. |
| missions-elites-zones | Salvage Jobs — Finding a Salvage Job (crew action + 1D6 availability table) | medium | There is no way to look for a Salvage job. The D6 availability table, the 2-credit Fee outcome and the Illegal-job outcome never occur, so a Salvage mission can never enter a campaign thr... | FIXED `740db7e36` — the D6 lived in `SalvageJobGenerator.find_salvage_job()`, which had ZERO callers repo-wide; the live producer `generate_salvage_job()` never invoked it. Rolling it closed three rules at once: roll 1 'no job this campaign turn' (a job had been offered EVERY turn instead of 5 in 6), rolls 2-3 the 2-credit non-refundable acceptance fee, and roll 6 the illegal job — whose `is_illegal` flag had no producer anywhere, making the p.137 authorities consequence unreachable. That consequence is now a real mandatory 3-option prompt after the post-game Rivals roll, not a line of text to remember. |
| missions-elites-zones | Salvage Jobs — Illegal Jobs post-game consequence | medium | Illegal salvage jobs never occur, and even if one did the panel would treat it as legal. The player is never fined, never has to forfeit Salvage, and never gains an Enforcer Rival — the e... | FIXED 0f10cbfcd (Aug 6 battle audit, sweeps W2-1/W8 — **this row was already stale when re-read**). `is_illegal` had no producer anywhere in `src/`, so `SalvageJobGenerator.gd:201`'s illegal branch was unreachable. Fixing it exposed that the ENTIRE p.137 D6 availability roll was inert: `find_salvage_job()` contained it and had zero callers, while the live producer `generate_salvage_job()` never called it — so a salvage job was offered EVERY turn (roll 1 = "no job available" never fired), the 2-credit non-refundable fee on 2-3 was never charged, and roll 6's illegal job could not occur. All three now roll, and the post-Rivals D6 authorities check is a mandatory 3-option prompt (`PostBattlePhase.resolve_illegal_salvage_choice`) rather than a line of text the player must remember to resolve. |
| missions-elites-zones | Salvage — the Scrapper trade (post-battle Step 4) and salvage-as-currency | large | Salvage units collected in a mission evaporate at the end of the battle — they are never tallied at Get Paid, never buy anything from a Scrapper, and never offset ship-repair/module/bot-u... | FIXED (Aug 6, final sweep) — new SSOT `src/core/campaign/SalvageLedger.gd`. Salvage is banked at post-battle **Step 4** (`PostBattlePhase` step 4d, after the p.138 authorities check so a caught crew cannot keep confiscated units), persists on `progress_data["salvage_units"]`, and is spendable two ways: the **Scrapper** (3 Loot Table rolls, each priced 1D6 with p.147's "treat a 1 as a 2", once per campaign turn, no credit top-up) via `ScrapperTradeDialog` from the purchase step; and as **currency for exactly three purchases** — ship repairs (`UpkeepPhaseComponent.repair_hull_points`), ship modules (`ShipManager`), bot upgrades (`AdvancementSystem.install_bot_upgrade`), each refunding the salvage if the credit half then falls short. p.147 "no Invasion checks after a Salvage battle" wired at `PaymentProcessor.process_invasion_check`. **Found while wiring: `is_illegal` and `salvage_units` had NO producer on `battle_result`** — so the p.138 authorities roll was unreachable even after its own Aug-6 fix, and `SalvageMissionPanel.get_salvage_units()` had zero callers. Both now ride `mission_data` through `BattleResultNormalizer`, so all four exits carry them |
| missions-elites-zones | Compendium DLC battle-setup instructions (AI Variations, Difficulty Toggles, Escalating Battles, Dramatic Combat, Grid-based Movement) reaching the battle screen | medium | With the Freelancer's Handbook enabled, the battle screen's COMPENDIUM DIFFICULTY RULES, ESCALATING BATTLES, DRAMATIC COMBAT and GRID-BASED MOVEMENT setup sections never render, and the p... | FIXED 0eead4056 |
| missions-elites-zones | Bestiary / Elite enemy reference data is loaded but never consulted | one-line | No direct gameplay effect today (Bestiary.json duplicates the Core Rules encounter tables that enemy_types.json already supplies), but ~25KB of JSON is parsed on every EnemyGenerator cons... | **FIXED - deleted.** `bestiary_ref` and `elite_enemies_ref` were assigned in `_load_enemy_data()` and read by NOTHING repo-wide, and `DataManager.load_json_file()` does not cache - it opens and parses on every call - so ~25KB of JSON was re-parsed on every `EnemyGenerator.new()`, which is once per battle (`CampaignTurnController`) and once per World Phase (`JobOfferComponent`). Deleted rather than made lazy because neither was the live source: the elite rules read `data/elite_enemy_types.json` through `CompendiumEliteEnemies` (which has its own load-once static cache) and the core tables read `data/enemy_types.json`. Both JSON files stay on disk as what `data/RulesReference/` is FOR - verified PDF extractions to check values against - but a second in-memory copy of the same tables beside the live one is a source-of-truth hazard, not a spare. |
| post-battle | Step 10 — Advanced Training: application fee, 2D6 4+ approval, course cost, and course record (p.124) | medium | In the post-battle wizard the player picks Pilot Training, presses Roll, sees "2D6 Roll: 9 - Training APPROVED!" and the log line "Kaya completed pilot training" — and nothing happens. No... | FIXED c942fec91 |
| post-battle | Step 13 — Character Event: Precursor "roll twice and pick either score" (p.126) | small | Any crew with a Precursor: whenever the randomly-selected character for step 13 is that Precursor, the Character Event is silently dropped — no XP, no story point, no rumor, no status eff... | FIXED (Aug 6) — **FOUR failures in series, and fixing any one alone would have proved nothing.** (1) The roll-twice branch resolved origin by handing a crew_id STRING to `PostBattleContext.get_character_origin()`, which takes a CHARACTER — `"origin" in "crew_1"` is a SUBSTRING test, so it fell through every branch and returned "Human" for everyone; the Precursor comparison was permanently false. (2) The whole pipeline — signal, public API, and a purpose-built `PrecursorEventChoiceDialog` — was attached to the step-12 CAMPAIGN Event, where the book has NO such rule (p.125 step 12 is one line; a whole-book sweep for "Precursor" finds the rule only at p.17 and p.126, both naming the Character Event), and it triggered on ANY Precursor in the crew rather than the selected one. (3) The correct step-13 producer had no consumer: its choice envelope carries no `type` key, so it fell through the orchestrator's `has("type")` guard and the event was dropped. (4) The UI auto-picked event 1 behind a "Deferred" note — rolling twice and always keeping the first is distributionally identical to not rolling at all. Now: ids resolve at the door (`PostBattleContext._as_member`), the rule lives on step 13 with a suspend/resume split (`_process_post_character_steps`), and the p.17 third option — avoid the event for 1 story point — is offered for the first time. `PrecursorEventChoiceDialog` deleted (zero callers, 2 options, wrong step); the prompt reuses `ItemChoicePopup`, which refuses to close without a selection, as a suspended pipeline requires. 23 new cases in `tests/unit/test_precursor_and_training_rerolls.gd`. |
| post-battle | Step 8 — Injury Table roll 16, Miraculous escape (p.122) | small | Rolling exactly 16 on the Injury Table — the single best non-XP outcome in the game — does literally nothing: the character gains no Luck point and keeps every item they were carrying. Th... | FIXED ca68e01b6 |
| post-battle | Step 8 — Injury Table roll 96-100, School of hard knocks: "Earn 1 XP" (p.122) | one-line | A crew member who rolls 96-100 after being downed gets nothing. The book's consolation prize for a bad battle is silently withheld every time. | FIXED ca68e01b6 |
| post-battle | Step 8 — Injury Table equipment consequences (rolls 1-5 and 17-30; Bot 1-5 and 16-30) (p.122) | medium | Equipment never degrades from injuries. A Gruesome Fate kills the character but leaves their gear pristine in the stash; a 17-30 Equipment Loss result damages nothing, so the Repair crew ... | FIXED bffe2c8d1 — the equipment flags were computed and stored on every injury and read by NOTHING. 20 of 100 on each table. Gruesome fate (damaged, repairable) and Miraculous escape (permanently lost) shared one flag and are different outcomes; split. Damage writes the {type: item_damaged, damaged_item} marker Repair Your Kit reads, so p.78 finally has something to repair. |
| post-battle | Step 5 — Battlefield Finds table entries 1-15, 16-25, 36-45, 46-60, 61-75 (p.121) | large | On the backend path five of the eight table entries (60% of the D100 range) award nothing — no weapon from the slain enemy, no consumable dosage, no starship part credit, no 1D3 debris cr... | FIXED (this commit) |
| post-battle | Step 9 — XP: "First character to inflict a casualty +1" and "Killed Unique Individual +1" (p.123) | medium | Playing the battle out in the app costs you XP relative to the book: the crew member who drew first blood and the one who killed the enemy Unique Individual each receive 2 or 3 XP instead... | FIXED (row was stale) — BattleResultsInputForm asks for both, as crew_id and as a LIST of crew_ids, matching the consumer shapes exactly; TacticalBattleUI builds that form as "the reachable record-what-happened path for a PLAYED battle at ANY tier". RESIDUAL, narrow: the AUTO-RESOLVE path cannot produce either (nothing derives first blood from a simulated fight), so an auto-resolved battle still under-pays by up to 2 XP. Tracked in the closeout plan, not as a live rules gap. |
| post-battle | Step 2 — Patron Status: One-time Contract exception, and Patrons lapse on travel unless Persistent (p.119) | medium | Patrons accumulate forever. Every completed job — including one-shot contracts that should evaporate — adds a permanent contact, and flying to a new world does not shed the old world's Pa... | FIXED fab705684+592a67212 |
| post-battle | Step 12 — Campaign Event 1-3 (Friendly Doc) and 45-48 (Equipment Malfunction) (pp.126-127) | small | The Friendly Doc event never shortens anyone's Sick Bay stay; the crew member is released on exactly the same turn as before. The Equipment Malfunction event never damages anything in the... | FIXED d945a3ecb + b14974c37 — Equipment Malfunction targets the STASH per p.127 (`damaged: true`, a flag Assign Equipment and Purchase Items already read with no producer) and Repair Your Kit now searches the stash as well as carried gear, since p.78 draws no line. Friendly Doc wrote `injury_recovery_turns`, which the countdown ignores — it decrements injuries[] and clears the bay when that is empty — so the stay never shortened. |
| post-battle | Step 13 — Character Events 11-12 (Time to Move On), 20-23 (Scrap with Crewmate), 63-66 (Hurt on Ship), 72-75 (Gift) (pp.128-130) | medium | Four Character Events (roughly 15 points of the D100 range) are pure flavour text. Nobody leaves the crew, nobody brawls, nobody goes to Sick Bay from ship maintenance, and the gift never... | FIXED — 11-12 Time to Move On (the 1D6-vs-recovery roll never happened, so nobody ever left Sick Bay), 20-23 Scrap (the Feeler and K'Erin clauses were wired and the FIGHT was not), 63-66 Hurt on Ship (already fixed), 72-75 Gift (the Loot roll never happened). Exposed that ctx.injure_specific_crew wrote nothing at all — a permanently-false has_method guard plus an `is_wounded` set that aborts on a Dictionary. |
| post-battle | Step 10 course benefits — Medical school and Bot technician injury rerolls (p.125) | large | Paying 20 XP for Medical school buys nothing — casualties still roll once on the Injury Table. Paying 10 XP for Bot technician buys nothing — Bots still roll once, and Bot upgrades cost f... | FIXED (Aug 6) — both rerolls now fire at the Injury Table roll sites. Underneath the row sat a **storage divergence**: the course was recorded under three different names. `TrainingSelectionDialog` granted `bot_tech`, `CharacterDetailsScreen` read the SSOT `data/training_courses.json` and granted `bot_technician`, and `AdvancementSystem` set `has_*_training` BOOLEAN properties that **do not exist on `Character`** — `Object.set()` on an undeclared property is a silent no-op, and `purchase_training` has zero callers anyway, so that whole mechanism was dead. `acquired_training` is the one live store. Dialog now writes the SSOT id; `Character.has_training()` accepts the legacy `bot_tech` for existing saves; `test_advanced_training` pins the dialog's ids against the JSON rather than a second hardcoded list. Medical school honours the book's constraints on the MEDIC (in the battle, not a casualty, Shuttle waives participation), nominates ONE casualty (picker on `BattleResultsInputForm`, asked pre-roll for the same reason as the Reduced Lethality exemption), and skips Bots (they roll on the Bot Injury Table). "Picking the better result" is ranked on consequences the book itself assigns — fatal, permanent stat loss, permanent equipment loss, equipment damage, Sick Bay upper bound — with ties keeping the first roll, so a reroll can never worsen an outcome. **Still open, tracked separately:** `RedZoneSystem.gd:62` reads `has_broker_training`, a property nothing can set (its trait fallback keeps Broker partly working), and `AdvancementSystem.purchase_training` is dead code. |
| post-battle | Step 13 — "If an event is completely inapplicable, simply add +1 XP to the character" (p.126) | one-line | When a Character Event cannot apply — a K'Erin drawing "All this endless violence is depressing you", an Engineer drawing "They don't make them like they used to", or any event whose titl... | FIXED (this commit) |
| turn-upkeep-travel | Ship Debt — payments, interest, and seizure (World Step 1) | medium | A crew that financed its ship at campaign creation (real saves carry ship.debt 12-36) pays 0 interest forever. The debt figure on the Ship screen never moves, never crosses 75, and the sh... | FIXED fc0f2d2c9 |
| turn-upkeep-travel | Pay for Medical Care — 4 credits removes 1 campaign turn of recovery | medium | An injured crew member always sits out the full injury-table recovery. A player with 40 credits and a character facing 4 turns in Sick Bay has no way to buy them back into the crew; the m... | FIXED fc0f2d2c9 |
| turn-upkeep-travel | New World Arrival Steps 1-3 — Check for Rivals, Dismiss Patrons, Check for Licensing Requirements | large | Every Rival you have ever made follows you to every new world forever — the Rival list only grows, which inflates the p.85 'roll a D6 vs number of Rivals' check into a near-certain forced... | PARTIAL fc0f2d2c9 (steps 1-2 done; step 3 Freelancer License open)  **CLOSED Aug 7** — step 3 DID NOT EXIST. `NewWorldArrival.apply()` implemented steps 1-2 and stopped, so no world ever required a Freelancer License and the forged-licence gamble was unreachable. Now: `roll_licensing_requirement()` rolls 1D6 on arrival (5-6 requires a licence, a further 1D6 sets the fee), keyed PER PLANET and rolled once — a world already checked is never re-rolled, or a reload would let the player retry until it came up unlicensed. A purchased licence is permanent ("for perpetuity, even if you return"). `attempt_forged_licence()` is 1D6+Savvy vs 6+, one attempt per world, and keys the Rival penalty on the NATURAL die ("if the roll is a 1 BEFORE MODIFIERS") — a high-Savvy character is no safer from being caught, which is the whole shape of the gamble. Gated at `JobOfferComponent._acceptance_block_reason()` and scoped to PATRON jobs exactly as the book scopes it, so p.85's always-available Opportunity mission keeps it from soft-locking the turn |
| turn-upkeep-travel | Starship Travel Events Table — all 16 D100 results | large | Traveling is mechanically free of risk and free of reward. Asteroids never damage the hull, Navigation Trouble never costs a story point, Raided never starts the pirate battle (or takes y... | FIXED (all 16 events) |
| turn-upkeep-travel | Upkeep shortfall — one crew member refuses jobs per credit short | small | Failing to pay Upkeep costs nothing. The player is shown a dialog naming the crew who 'refuse to work this turn', then those exact characters appear enabled in the Crew Tasks list and can... | FIXED 95c35bdd0 |
| turn-upkeep-travel | Recruit crew task — a new character actually joins | one-line | Sending one or two crew to Recruit prints 'Automatic recruit (crew below 6)' or 'Roll 4 -> 6 vs 6' in the results panel and the crew roster is unchanged. A crew reduced to 3 members by ca... | FIXED 95c35bdd0 |
| turn-upkeep-travel | Spending credits for +1 on crew tasks (Find a Patron, Track, Repair) and the 3-credit extra Trade roll | medium | There is no way to spend a single credit to improve a Patron search, a Rival hunt, or a Repair, and no way to buy the extra 3-credit Trade Table roll. The book's main credit sink during t... | PARTIAL 8210675ca (+1-per-credit done; the 3-credit extra Trade roll still open)  **CLOSED Aug 7** — p.78's "You can get additional rolls by spending 3 credits each. At least one crew member must be Trading to permit this expenditure" had NO UI at all, and the p.73 Busy Markets 2-credit roll had an accessor with zero callers. Both now buy real rolls through the SAME `_resolve_table_task` pipeline as the free ones (the 100-row Trade table must not fork). Counted separately: different price, different limit, different precondition — merging them would let the Busy Markets roll bypass p.78's someone-must-be-Trading gate. Busy Markets is once per campaign turn, persisted so leaving and re-entering the step cannot repeat it, and reset at rollover — a per-turn cap with no reset silently becomes a per-CAMPAIGN cap |
| turn-upkeep-travel | Patron job Time Frame — job fails if not completed in time | medium | A Patron job stamped 'This campaign turn' can be shelved for twenty turns and then completed for full pay. Patron jobs never expire, never count as failures, and never trigger the p.84 'V... | FIXED fab705684 + cfbd4f91f (row was stale) — Time Frame was wired with the rest of the p.83 job data: offers persist on the campaign with a real deadline_turn, JobOfferComponent expires them at turn start, and Vengeful (p.84) fires on the lapse. Duplicate of the BHC row at L50. |
| turn-upkeep-travel | Ship wreck — accumulated Hull damage destroys the ship | small | A ship reduced to 0 Hull Points is not a wreck — it is merely grounded, and the free 1-point-per-turn repair at rollover (CampaignPhaseManager.gd:673) floats it again next turn. The crew ... | FIXED 95c35bdd0 |
| turn-upkeep-travel | Emergency Take-off (p.60) — 3D6 hull damage for insisting on travel while damaged | small | The live Travel button is simply DISABLED while the hull is damaged, so the player never gets the book's choice; get_emergency_takeoff_damage() is called only from the dead TravelPhase.gd. Also the only in-space producer for the p.59 wreck branch. | FIXED |
| turn-upkeep-travel | progress_data["crew_retired"] — campaign archival on crew retirement | one-line | A campaign that ends because the crew retired (rather than by victory or by reaching 20 turns) is never archived to LegacySystem — the crew, story points and turn count are silently dropp... | **FIXED - the flag had a reader and no writer.** `EndPhasePanel._try_archive_campaign()` has always gated archival on `progress_data["crew_retired"]` (:224) and NOTHING in the codebase ever wrote it, so the Core Rules p.8 ending - 'just play until they feel their crew is either forced to disband, or has made it big and can retire' - dropped the campaign silently. Added the missing writer as a real control: a confirmed **Retire the Crew (End Campaign)** button that writes the flag on the CAMPAIGN (so it survives a save), saves, archives, journals a `crew_retired` milestone, locks the phase buttons and emits `campaign_retired`. The archive now carries `ended_by` (victory > retired > ended - a crew retiring on its winning turn still earned the p.65 Elite Rank) and the Hall of Fame card renders RETIRED distinctly, so the new key has a consumer rather than being another write-only field. **Reading that function surfaced a second defect beside it:** archival had no idempotence guard while `setup_phase()` re-enables Save every turn and the `turns >= 20` gate never closes again - a campaign taken to turn 30 appended ELEVEN copies of itself to the same list `get_best_campaign()` and `get_legacy_bonus()` read. Guarded with `campaign_archived`, claimed AFTER the LegacySystem null-check so a missing autoload cannot burn the one attempt. 11 cases in `tests/unit/test_campaign_retirement_archive.gd`, detection-proven by removing `ended_by` from the whitelist (3 failures). |
| turn-upkeep-travel | 6 | World Traits now PARTIAL — 12 traits need a call site  **CLOSED Aug 7** — see row 239 |
| battle-setup | 8 | deployment conditions, seize initiative |
| post-battle | 11 | Crippling Wound, injury equipment loss |
| patrons-rivals-quests | 12 | Time Frame never expires; the 30 p.83 BHC subtable entries |
| economy-trade-equipment | 13 | gun mods, on-board items, Merchant reroll |
| factions-world-compendium | 20 | DLC-gated; a tester without the DLC never sees any of it |
| missions-elites-zones | 20 | Black Jobs, Red Zone conditions, Elite enemies |

## WRONG-VALUE (21)

| Domain | Rule | Fix size | Player-visible effect | Status |
|---|---|---|---|---|
| factions-world-compendium | Faction generation quantity/types — Compendium p.110 | small | If faction init is ever wired as-is, the player gets 8-16 factions named "United Federation", "Skull Raiders", "Quantum Hive" etc. instead of the book's 2-4 Merchant cartels / Criminal en... | FIXED (Aug 6) — **and the row's "if it is ever wired as-is" hypothetical is exactly what was avoided, so wiring it had to fix this first.** `_initialize_default_data()` no longer creates factions at all (a comment at the site says why, so nobody reinstates it); `generate_world_factions()` is the sole entry point and rolls 1D3+1 with types from the p.110 D100 table. Two further defects surfaced while wiring it: (1) `_parse_roll_range` read the d100-wheel convention "91-00" as `[91, 0]`, so `roll >= 91 and roll <= 0` never matched and **Secretive organization — 10% of the table — could never be rolled**; (2) faction ids are derived from names drawn from a 5x5 prefix/suffix pool per type, so two of a world's factions could collide and the second would silently overwrite the first, handing the player 3 factions where the book rolled 4. Both fixed and pinned by test, including a row asserting every roll 1-100 lands on exactly one type. |
| factions-world-compendium | Name Generation tables — Compendium pp.157-160 | medium | Every corporate patron in every campaign was named off a D100 table that is in no rulebook, and eight invented generators sat in the data file presenting as rules. | FIXED (Aug 6, found by request — this row did not exist) — the Compendium's Name Generation section has **exactly four** generators: World Names p.157, Colony Names p.158, Ship Names p.159, Corporate **Patron** Names p.160. `NameGenerationTables.json` carried **twelve**. World / Colony / Ship are book-faithful (World is byte-exact 25/25 against the PDF, including the p.157 worked example "43 and 3 → Gough III"). The book's Corporate Patron table was **absent entirely**, and a fabricated "Corporate Names Generator" stood in its place sharing not one entry with it — book Part 1 opens Interstellar / Agile / Calibrated / Synergistic, the fabrication opened Unity / Stellar / Fringe / Core. The remaining eight (Gang, Rival, Faction, Enhanced Ship, Enhanced Planet, Starport, Space Station, Sector) appear in neither book. **Worst part: the fabricated tables were the LIVE ones** — `CharacterGeneration` and `ContactManager` load exactly three titles by exact string, and all three were fabrications. Corporate Patron restored verbatim from p.160 and both consumers repointed; the six dead fabrications deleted; Gang and Rival kept ONLY so those NPCs get a readable label and tagged `source: "APP_FLAVOR - not a rulebook table"`, since the books offer no replacement. Every generator now carries a `source` and a test refuses any entry that is neither page-cited nor APP_FLAVOR-tagged — that missing field is how eight invented D100 tables passed as rules. 8 cases in `tests/unit/test_name_generation_tables.gd`. |
| factions-world-compendium | Instability tracking — Compendium p.148 | medium | Were the arrival gate fixed, a Chaos event would fire on roughly half of ALL World Phases forever (a fresh 4+ D6 each turn) instead of once every two-to-three turns of built-up Instabilit... | **STALE - verified fixed** by the same commit. Instability is a persisted running total, not a fresh roll each turn: it starts at +1 on arrival, accumulates 1D6 plus the three book modifiers each Invasion step, and the D100 Chaos table fires only at >= 10, after which the score is reduced by the row's own listed amount. Pinned by `test_the_accumulator_applies_all_four_book_modifiers`, `test_the_d100_fires_only_at_ten_or_above`, `test_below_the_threshold_nothing_fires`, `test_firing_reduces_the_score_by_the_row_amount` and `test_a_quiet_turn_can_reduce_instability`. |
| factions-world-compendium | Compendium page citations shown to the player | one-line | A player who ticks 'Expanded Factions' in the campaign wizard and turns to Compendium pp.148-153 to read the rules lands in the Fringe World Strife and Loans chapters and finds nothing ab... | **STALE - verified against the PDF.** `ExpandedConfigPanel` already cites Expanded Factions as **pp.110-115**, not 148-153. All six Compendium setup-flag citations shown to the player were re-checked page by page against the book this session and every one is correct: Factions p.110 (chapter opening), Loans p.152 (ship loans), Fringe World Strife pp.148-151, Dramatic Combat p.87, Casualty Tables p.99, Detailed Post-battle Injuries p.101. |
| economy-trade-equipment | Post-Battle Step 7 "Gather the Loot" — one roll per battle | medium | Every battle yields TWO independent Loot Table results instead of one — one silently added by the backend, one the player rolls in the wizard. A Quest finale yields six items instead of t... | **STALE — verified not reproducible.** The two roll sites are real (backend step 7 + the wizard's button) and both run on the live path — `CampaignTurnController` calls `start_phase(POST_MISSION)` from six sites. But `start_phase` emits `phase_changed` BEFORE `_execute_phase_start()`, so the wizard is already showing when the backend runs, and `post_battle_substep_changed(GATHER_LOOT=7)` syncs `current_step = 6` immediately before `loot_gathered` fires — so `_on_backend_loot_generated`'s `current_step == 6` arm DOES run, disabling the button and auto-satisfying the inline roll. One result per battle. Pinned by 3 new ordering cases in `test_post_battle_wizard_honesty` (the enum→step mapping, the emit order, and the handler doing BOTH the increment and the disable). Corrected a docblock at `CampaignPhaseManager.gd:1362` that asserted the interactive flow never runs the orchestrator — it does, every battle. |
| economy-trade-equipment | Loot Table three-roll procedure — category, subtable, then the exact item by D100 (Core Rules pp.131-133) | medium | Loot item frequencies are wrong across the board. Grenade loot is 50/50 Frakk/Dazzle instead of the printed 60/40. Within melee loot a Blade drops to 12.5% from 20% while a Suppression Ma... | **FIXED** — the third roll did not exist as DATA. `loot_tables.json` leaf subtables stored a flat `items: [name,...]` list with no ranges, so all FIVE resolvers picked uniformly (LootTableResolver, LootSystemConstants, CrewTaskComponent, CrewTaskEventDialog, PostBattleSequence). Added the book's per-item D100 ranges for all 12 leaf subtables (pp.131-134, verified tiling 1-100 with no gap/overlap) and made `LootTableResolver.roll_item_in()` the single roller all five call. Also fixed two wrong-table branches found alongside: `CrewTaskEventDialog` answered "Low Tech Weapon" out of the LOOT melee subtable (Power Claw / Suppression Maul instead of the p.28 table) — the identical bug fixed in CrewTaskComponent months ago and left standing here — and both files answered "Gadget" with merged gun_mods+gun_sights instead of the p.29 Gadget Table. `test_loot_table_distribution` 8 cases, detection-proven (reverting to uniform fails exactly the 3 distribution rows + the source-shape row; the 3 data rows still pass, which is why the suite is split). |
| economy-trade-equipment | The p.28-29 tables referenced by the Trade Table (Low-Tech Weapon Table, Gear Table, Gadget Table) | medium | Trade Table 1-3 "A personal weapon", which should be a Handgun/Scrap Pistol/Colony Rifle/Shotgun/Blade etc., instead returns a Power Claw, Suppression Maul, Glare Sword, Ripper Sword or B... | FIXED e2e56b985 |
| battle-setup | Number of Opponents — Insanity adds +1 to the final number faced | one-line | On Insanity — the hardest mode in the book — every battle fields one fewer enemy than the rules require. Combined with the danger_level defect above the +1 could not fire regardless, so f... | FIXED 24c657af4 |
| battle-setup | Determine Deployment Conditions — consult the column matching the mission type; the table is ignored during an Invasion battle | small | Two concrete wrong outcomes. (1) Every Rival battle rolls deployment conditions on the Opportunity/Patron column instead of the Rival column, so the odds are badly skewed — 'No Condition'... | FIXED 565ddc4b5 (read mission_source not source; Invasion skip added) |
| battle-setup | Determine the Enemy — roll the Enemy Encounter Category on the column matching the mission (Opportunity / Patron / Quest / Unknown Rival) | medium | A Rival that tracks you down is generated off the Opportunity or Patron column, so 20-25% of Rival battles are against Roving Threats — a category the book excludes from Rival fights outr... | FIXED 565ddc4b5 (the silent tables.get("patron") fallback sent a quarter of Rival battles against wildlife) |
| patrons-rivals-quests | Danger Pay is a PATRON-job payment only — "If you did a Patron job, add the Pay bonus to the Danger Pay" (Core Rules p.120 Step 4) | one-line | Opportunity missions — the default 'nothing else presented itself' battle — pay an extra 1 to 3 credits of Danger Pay they are not entitled to. On a single-digit credit economy where Upke... | FIXED 878057d6a (gated at the offer builder AND the payment step) |
| missions-elites-zones | Red Job Improved Rewards — Quest credit payout | small | Finishing a Quest in a Red Zone pays slightly more than the book allows — best-of-4 averages ~5.24 vs the book's best-of-3 ~4.96, i.e. roughly +0.3 credits per Red Zone quest conclusion, ... | **FIXED - and the rule was more general than the row.** Every rule that widens the p.120 pay roll states a TOTAL, not an increment, so when two apply the answer is the LARGER total and never the sum. p.120 quest finale = two dice +1; p.150 Red Zone general = two dice; p.150 Red Zone Quest = **three** dice +1; p.83 Danger Pay 10+ = two dice. The p.150 Quest bullet is the tell - if these stacked it would be redundant with the general Red Zone bullet printed one line above it and would read "roll one ADDITIONAL die". The code applied them in sequence, so a Red Zone Quest conclusion rolled FOUR dice (best-of-4 averages 5.24 vs the book's best-of-3 4.96), and Danger Pay could have stacked a fifth. Rewritten as a die COUNT so the four cases read side by side and a future bullet cannot silently stack again; Danger Pay uses `maxi()` for the same reason. Each die still routes through `roll_credit_die()` so Compendium p.32 Money is Tight applies to all of them, not just the first. Pinned by a 4000-trial expected-value ladder (3.50 / 4.47 / 5.47 / 5.96 at +-0.12) which also proves best-of-4 at 6.24 is separable, so the assertion can really fail. |
| post-battle | Step 13 — Character Event: exactly ONE roll, for ONE randomly selected character (p.126) | medium | A six-person crew resolves SEVEN character events per campaign turn instead of one — six applied by the player plus one applied silently by the backend. XP, story points, quest rumors, Ri... | FIXED ca68e01b6 |
| post-battle | Step 5 — Battlefield Finds: "Roll D100 ONCE on the table below, and add the resulting find to your inventory" (p.121) | small | Two Battlefield Finds are resolved per battle. The player sees the backend's find in the log (which may hand out a Quest Rumor or +1 credit) and then rolls a second, different find that i... | FIXED ca68e01b6 |
| post-battle | Step 5 — Battlefield Finds 76-90, Vital info: automatic Corporate Patron (p.121) | small | A 76-90 Battlefield Find against a normal enemy hands the crew a Quest Rumor instead of a free Corporate Patron. The player loses a standing job source on that world and instead gets prog... | FIXED (this commit) |
| post-battle | Step 13 — Character Event eligibility: "Any character is eligible, as long as they are part of your crew, even if they are in Sick Bay" (p.126) | one-line | A crew member in Sick Bay can never draw a Character Event, which is precisely backwards for the several table entries written for them — "You are starting to wonder if it is time to move... | FIXED (this commit) |
| post-battle | Step 9 — "Any character that flees the battlefield in the first 2 rounds of the battle receives no XP" (p.123) | small | It is all-or-nothing. If the flag is set, the whole crew — including the four members who fought to the end — earns zero XP for the battle. If it is not set, a character who Bailed on rou... | CORRECTED — finding was half right: fled_early means a WHOLE-CREW withdrawal, so zeroing all is correct there. Real gap is per-character bail tracking, which no producer emits. Read side wired for fled_early_crew. |
| post-battle | Step 8 — the injury the player is shown is not the injury that is applied (p.121) | medium | The player rolls their casualty's injury, sees "Rolled 88 - KNOCKED_OUT", clicks past it — and the character is actually dead, or in Sick Bay for 6 turns, because the backend rolled somet... | FIXED ca68e01b6 |
| turn-upkeep-travel | Commercial passage does not roll Starship Travel Events | one-line | A shipless crew buying passage on a liner is shown 'Asteroids — your ship takes Hull damage' and 'Drive trouble — your ship is grounded' events about a ship they do not own. (The mechanic... | FIXED 95c35bdd0 |
| battle-resolution | Weapon Ratings table (p.50) — Range / Shots / Damage / Traits | medium | The in-battle Weapon Table reference card gives wrong stats for most weapons. A player firing a Plasma Rifle is told 18" 1 shot Damage 2 when the book says 20" 2 shots Damage 1 — half the... | FIXED 91398d133 — the CARD was wrong in 6 of 10 rows (Plasma Rifle had wrong shots, damage AND traits) and WeaponTableSystem kept an 89-line fabricated FALLBACK table (~30 wrong profiles, invented traits Burn/Overheat/Stabilize/Silent, invented weapons) that fed WeaponTableDisplay and EnemyGenerationWizard on any JSON load failure. Its Suppression Maul matched the COMPENDIUM Game Options table, not the Core Rules. data/equipment_database.json verified byte-correct for all 32 book weapons; fallback deleted, failure is now loud |
| battle-resolution | To Hit (p.44) — 1D6 + Combat Skill vs the target number | one-line | Every ranged shot rolled through the in-battle Quick Roll tool is easier by the shooter's full Combat Skill. Combat Skill +1, target in the open at 12" with a 24" rifle: the book needs a ... | FIXED 4b7102dd0 — and found a LIVE BUG: Combat Skill was applied TWICE (subtracted from the threshold AND added to the roll), so +2 skill shot at an effective +4 |
| battle-resolution | Brawling — the 2" bonus move is earned only by ELIMINATING the opponent (p.45) | one-line | Every won Brawl grants the crew member a free 2" repositioning move, even when the loser merely got Stunned and is still standing there. Over a battle this hands the player several inches... | FIXED 7edee08cd — resolve_brawl() reported no post-Brawl move at all, so the rule was unavailable. Now gated on the casualty count, never the winner field, and records that it cannot enter a new Brawl |

## FABRICATED (13)

| Domain | Rule | Fix size | Player-visible effect | Status |
|---|---|---|---|---|
| factions-world-compendium | exploration_value / exploration_progress — no such mechanic in the Compendium or Core Rules | small | Any UI bound to `exploration_progress` shows a world creeping toward '100% explored' after ten missions, a statistic that exists in neither rulebook and that no rule consumes — misleading... | **FIXED - removed under the fabricated-data rule.** "Exploration progress" appears in NEITHER rulebook. No rule consumed it, its `exploration_progress_updated` signal had **zero listeners**, and it read `mission_data["exploration_value"]` - a key with no producer anywhere - so it defaulted to a flat 0.1 per mission and displayed a world as "100% explored" after ten missions: a completion bar for a completion mechanic that does not exist. Deleted the property, both serialize/deserialize lines, the signal, the accrual in `complete_mission()`, the `get_planet_stats()` field, and BOTH display sites (`PlanetDetailBuilder`'s info row and CampaignDashboard's 31-line `_add_exploration_bar()` progress bar, now zero-caller). The legitimate `missions_completed` and `resources_extracted` counters are untouched and test-pinned. |
| economy-trade-equipment | Selling items — 1 credit each, up to 3 per turn, no condition restriction (Core Rules p.125) | small | Today the restriction is inert (nothing writes the `damaged` key — see the damaged-loot finding), so it is invisible. The moment damage is wired correctly, a damaged Frag Vest or damaged ... | **FIXED**, and the row understated it — the restriction was NOT the worst part. (a) The damaged filter is gone: p.125 is "You may sell up to 3 items, earning 1 credit for each" with no condition qualifier, and damaged loot is now flagged `needs_repair` so the landmine this row predicted was about to arm. (b) The list was a FILTERED view indexed by row number into the UNFILTERED array, so any filtering sold the wrong item — now an explicit `_sell_row_to_stash` map. (c) **Selling paid but never removed**: `_on_sell_pressed` mutated `stash_items`, a `.duplicate(true)` local copy, then credited the campaign directly — the same item could be sold three times a turn, every turn, forever, while the stash never shrank. Now routed through `EquipmentManager.sell_equipment()`, the same call the p.76 sell-for-upkeep dialog makes (it credits internally, so the local `add_credits` is gone — it was double-paying). (d) The Sell pane was EMPTY in every post-battle Purchase Items step: `initialize_purchase_phase()` ran BEFORE `add_child()`, so every `@onready` was still null and `_populate_sell_items()` bailed at its own guard — the same shape as the battle audit's `_populate_deployment_conditions` defect. |
| economy-trade-equipment | Equipment prices and repair costs (Core Rules p.78 Repair, p.125 Purchase Items) | large | Opening Manual Select from the equipment panel shows a shop quoting 500–2500 credits per item, a 60%-of-value sell price, and a paid repair service with Quick/Quality tiers — none of whic... | FIXED (uncommitted) — VERDICT CONFIRMED against p.125 + p.78 and the fabricated economy is DELETED, not rescaled (CLAUDE.md: a mechanic the book lacks is fabricated, and fabricated means REMOVE). `src/ui/screens/equipment/EquipmentManager.gd` lost 515 lines: `_calculate_buy_price()` with a base of **500 credits**, `_calculate_sell_price()` at **60% of buy**, `_calculate_repair_cost()` returning **2500**, Quick (+50%) / Quality (+100%) repair tiers, the market generator and every buy/sell/repair handler. The `TradeButton` and `RepairButton` nodes AND their two `[connection]` entries are removed from the `.tscn` as well — leaving them would have left two dead buttons, which is precisely what `lint_tscn_connections` exists to catch (it is CLEAN after the change). **The reach made it worse than a stray screen**: the entry point is the campaign-creation wizard's "Manual Select" button, so the shop was quoting 500-2500cr to a crew holding single-digit credits. The assignment half of the screen — `set_crew_data()`, `_assign_equipment_to_crew()`, the crew/detail panes — is untouched, because picking gear for a crew member is what Manual Select is actually for. The book prices already live in `PurchaseItemsComponent` (3cr per table roll / 1cr basics / 1cr sell) and Repair Your Kit already lives in `CrewTaskComponent` as the FREE p.78 crew task. Pinned by `tests/unit/test_equipment_economy_is_book_only.gd` (5 cases, asserting the banned NUMBERS as well as the function names). |
| economy-trade-equipment | Trading/market economy (Core Rules p.125 Purchase Items; p.79 Trade Table) | small | No direct effect — the system never runs. The harm is that it is a large, convincing, wrong second source of truth for prices sitting next to the correct 1cr/3cr constants, and it is inst... | FIXED (uncommitted) — **the row's premise was wrong in the direction that mattered.** It read "the system never runs"; `TradingSystem.new()` was in fact called at `PurchaseItemsComponent.gd:72` on **every World Phase entry**, loading its JSON each time. The instance was then never used — `trading_system` appeared exactly three times in the file (preload, declaration, construction) and zero methods were ever called on it. So it cost load time AND sat next to the correct 1cr/3cr constants as a convincing second source of truth. All 828 lines are fabricated: market volatility, price fluctuation, per-world-type market conditions, rare-item chances, supply/demand — none of it in either rulebook, against a p.125 that is three flat numbers. `src/core/systems/TradingSystem.gd` DELETED with its `.uid`. **Collateral, same class:** `UpkeepSystem._get_credits()` / `_set_credits()` each opened with a "PRIORITY 1: Use EconomySystem if available" branch gated on `economy_system`, a field declared `= null` and **never assigned anywhere in the repo** — permanently false, so the FALLBACK was always the real implementation while the dead branch made a fabricated 842-line supply/demand economy look like the credit authority. Both branches and the field are gone. ⚠ **NEW FINDING, recorded not fixed:** `src/core/systems/EconomySystem.gd` (842 lines, markup/markdown × location × market × global multipliers) is now provably **test-only** — after removing those dead guards its only remaining references are 4 test files. It belongs to the existing tier-7 wire-or-delete backlog in `docs/WIRING_CLEANUP_BACKLOG.md`, which CLAUDE.md explicitly says not to bulk-delete; deleting it also means deleting `tests/helpers/EconomyTestHelper.gd` and 3 suites that assert its fabricated formulas. 66/66 green across all six economy suites after the change; all four wiring lints CLEAN. |
| patrons-rivals-quests | Find a Patron gates whether a job offer exists at all — "If the result is a 5 or higher, you've found a Patron to hire you for a job… If one job is offered, it will always be a random, existing Patron" (Core Rules p.77) | large | You never need to send anyone to Find a Patron: job offers appear every turn regardless, and once you have 4-5 Patrons (which never expire — see the travel finding) the list is 6-12 jobs ... | FIXED (uncommitted) — VERDICT CONFIRMED. p.83 opens "**If you received a job offer from a Patron**, you need to determine the details of the job", and p.77 is what produces that offer: 1D6 + crew looking + 1 per existing contact + credits, 5+ = one job, 6+ = two. `JobOfferComponent` handed EVERY Patron on the list a fresh job every campaign turn with no roll anywhere — so the task gated nothing, and since Patrons only lapse on travel (p.72) a 4-5 contact list was a permanent wall of work. **The task half was wrong too, in the opposite direction**: both the 5+ and 6+ branches created NEW Patrons, when p.77 says "If ONE job is offered, it will always be a random, EXISTING Patron. If TWO jobs are offered, one will be a random, existing Patron, the other will be from a NEW Patron." Now: `_grant_patron_job_offers()` banks an entitlement in `progress_data["patron_offers_owed"]` and creates a new Patron only for the second of two; `JobOfferComponent._consume_patron_offers_owed()` SPENDS it (the World Phase wizard allows back-navigation, and a counter that survived reading would mint a fresh job on every re-entry — the same unlimited-work bug through a different door) and shuffles the eligible list because the book says "a RANDOM, existing Patron", not first-in-list. p.85's "Carry out an Opportunity mission — ALWAYS AVAILABLE" fallback is what makes the gate safe rather than a soft-lock and is explicitly pinned. The Trade/Exploration "gain a Patron" table results keep their own `_add_patron_contact()` — they grant a CONTACT, and routing them through the job path would hand out free jobs through the back door. |
| missions-elites-zones | Expanded Missions — Special Conditions are Patron Jobs Only | one-line | An Opportunity mission can arrive carrying "No Psionics may be deployed", "No Armor may be deployed" or "No more than 3 crew may fire a weapon each round" — restrictions the book applies ... | **FIXED - one line, and the precedent was already in the file.** Compendium p.76 heads this table **"Special Conditions: Patron Jobs Only"** and opens it *"If undertaking a Patron job, roll D100 on the table below to determine what special conditions apply"* - the restrictions are justified in the book as "employer parameters". `_enhance_job_with_compendium()` rolled it for EVERY offer including Open Market ones, so an Opportunity mission could arrive banning Psionics, Armor, or more than 3 crew firing per round on behalf of an employer that does not exist. Gated on `mission_source` in {patron, faction} - the SAME test the Danger Pay fix at :633 already uses, because both are p.83/p.76 PATRON tables the Open Market builder was running anyway and two different answers to "is this a Patron job?" in one file is how a rule ends up half-applied. A test pins the two gates in agreement. |
| missions-elites-zones | Stealth Missions — Reinforcements after the alarm; Stealth-round enemy behaviour; spotting modifiers; sentry profile; completion rewards | medium | Currently invisible because the Stealth/Street panels never instantiate (finding 2). The moment those are wired, the companion will instruct the player with non-book numbers: too few rein... | **STALE - the prediction did not come true; data verified page by page against Compendium pp.118-122 this session.** The row was written while the Stealth panel could not instantiate (the `initialize_battle` early return, fixed Aug 6) and predicted that once it did, the companion would show non-book numbers. It does not. SEVEN rule blocks checked against the PDF and every one is verbatim: **reinforcements** (p.122 "At the beginning of each battle round, roll 2D6. For each die showing a 6, a basic enemy arrives... placed immediately, but cannot take any actions in the battle round in which they arrive"); **enemy count** (p.122 crew size +1, "These will include 1 Specialist and 1 Lieutenant", Roving Threats treated as Hired Muscle, reroll Rampage AI); **spotting modifiers** (p.121 -1 scanning, -2 partially obscured, -1 per intervening feature) INCLUDING the exceeds/equals/below three-way and the equals-case "suspicious" branch that advances the enemy 1 inch and re-rolls; **individual profile** (p.119 Reactions 1 / Speed 4in / Combat +0 / Toughness 4 / Savvy +1); the **objectives D100** (p.118, all six rows); **finding the target** (p.118, 6in of centre, minimum 3 candidates, 1D6+Savvy 6+, most distant point, last candidate automatic); and **psionics** (p.122). The "completion rewards" sub-claim has no referent - the stealth chapter ends at p.122 and Street Fights begins on p.123, so stealth missions take the ordinary post-battle rewards. |
| post-battle | Step 12 — Campaign Event: "Roll D100 on the Campaign Event Table. Apply the result immediately." (p.125) | small | The wizard's Campaign Event step shows "Rolled 73 - Minor positive event" — a result that is not in the rulebook and that does nothing. Meanwhile the actual Core Rules event (e.g. "Tax Ma... | FIXED 34520cb51 |
| post-battle | Step 9 — Character Upgrades: the Ability Increase Table is spent, not rolled (p.123) | medium | Step 9 of the wizard tells the player to "Roll for advancement" and prints results like "Rolled 5 - …" that change nothing. The actual, correct XP-spend UI is buried on the character shee... | FIXED 56714e6c2 — the D6 'advancement roll' awarding 'skill points' was a FABRICATION, not a broken feature: no such roll and no such currency exists. Deleted per policy and replaced with the real p.123 XP spend through CharacterAdvancementService. |
| post-battle | Step 1 — Resolve Rival Status: the two D6 checks (p.119) | small | The wizard's first step teaches the player a rule that does not exist and makes them roll it once per Rival, with no effect on anything. The real outcome (a new Rival on a 1, or a Rival r... | FIXED 34520cb51 |
| battle-resolution | In-battle rules reference (CheatSheetPanel) vs pp.44/46/40/51 — to-hit, Aim, damage resolution, armor saves, Stun, Suppression | medium | A player using the app's Reference tab at the table plays a different game. They roll 4+ flat instead of 3+/5+/6+; they add +1 for Aiming instead of rerolling 1s; they compare the weapon'... | FIXED bfa8afa67 — six sections rewritten; it taught a flat 4+ with no Combat Skill, Combat armor at 4+, a 'Powered 3+' that does not exist, and a morale system where no line matched the book |
| battle-resolution | To Hit modifiers (p.44) — the table has exactly three rows and no elevation or over-range modifier | small | A player who ticks "Elevated" on the shooting helper is handed a +1 that the rules never grant — a covered target at range drops from 6+ to 5+ purely for standing on a rooftop. Setting th... | OPEN — VERDICT CONFIRMED against the book. p.44 is EXACTLY three rows: within 6" and in the open 3+; within weapon range and in the open OR within 6" and in Cover 5+; within weapon range and in Cover 6+. Reprinted identically on the reference card and in Appendix XI. No elevation row, no generic over-range row. The ONLY legitimate to-hit modifiers are the *Heavy* trait (-1 if the firer moved), *Snap shot* (+1 within 6") and the **Bipod** gun mod (+1 at ranges over 8" when Aiming or firing from Cover, non-*Pistol* only) — an over-range bonus that is a MOD, not a table row. **FIXED 0f10cbfcd (Aug 6, audit F8)** — the checkbox is deleted from `CharacterQuickRollPanel`, along with the "Elevated"/"Ground" breakdown line that implied an effect. `BattleCalculations.calculate_hit_threshold` keeps the two `_`-prefixed elevation params for signature compatibility with its four callers; a note at the site records that they are correctly inert. |
| battle-resolution | Suppression — no such mechanic exists in the Core Rules | small | The in-battle rules reference teaches the player a status effect that does not exist in Five Parsecs, complete with a movement restriction and a -1 firing penalty. A player following the ... | FIXED 951cf1969 + 859b28429 (was PARTIAL; re-verified CLOSED Aug 6) — VERDICT CONFIRMED, dead sites purged. Every "suppress*" hit in the Core Rules is the Suppression maul weapon, "Pain suppressor" or the "Emo-suppressed" background; the Compendium adds only "Suppressing fire" (Renegade Soldier +1 shot). Deleted: BaseBattleRules.SUPPRESSION_MODIFIER, FiveParsecsCombatData.SUPPRESSED_PENALTY, EnemyTacticalAI.SUPPRESSION_PATTERN. STILL OPEN (live sites): BattleCalculations.gd:137/160/207, BattleResolver.gd:190/212/688-689, CharacterQuickRollPanel.gd:583, BaseCharacterResource.is_suppressed()/can_suppress(), the CheatSheetPanel text, and the SUPPRESSED enum members in BOTH enum files (deprecate in place — deleting shifts ordinals and breaks save compat, per the DifficultyLevel precedent). NOTE: "Emo-suppressed" is a REAL background (Character.gd:665-671, p.15 no-Luck rule) — do not remove it. **CLOSED — re-verified Aug 6, the "STILL OPEN" list above is STALE.** Every site named in it is gone: `BattleCalculations` deleted `SUPPRESS_THRESHOLD`, `check_suppression()`, `get_suppression_penalty()` and the fabricated `suppressive` weapon trait (each with the reason written at the site); `BaseCharacterResource` deleted `is_suppressed()`/`can_suppress()` plus four siblings; `BattleResolver` and `CharacterQuickRollPanel` have zero hits; the `SUPPRESSED` enum member is absent from BOTH enum files, so the deprecate-in-place caveat no longer applies. `CheatSheetPanel.gd:490-491` now teaches the CORRECTION — "There is no Suppression in Five Parsecs. The only status effect is Stunned." Every remaining `suppress` match in `src/` is a removal comment, the real Suppression maul / Pain suppressor / Emo-suppressed / Suppressing fire, or ordinary English (rival-encounter suppression, silencer). |

## PARTIAL (30)

| Domain | Rule | Fix size | Player-visible effect | Status |
|---|---|---|---|---|
| factions-world-compendium | Faction Activities timing and trigger — Compendium p.113 | small | Doing a job for a faction never triggers the guaranteed Faction Struggle that the book makes mandatory, so the power balance never shifts in response to the crew's own work. Faction activ... | **FIXED** - two defects. (a) `faction_job_id`, which `PostBattlePhase:554` passes to `process_faction_activities()`, had **no producer anywhere**; it is now stamped in mission_dict and - per p.113's "This does not occur for affiliated jobs" - deliberately left empty for an affiliated Patron job, so it is NOT simply a copy of `faction_id`. (b) The mandatory Struggle was gated on `power >= 3`, but the book says "If you did a job directly for a Faction, **always** perform the Faction Struggle event" - the Power 3+ requirement belongs to the D100 row 61-75 ACTIVITY. `_faction_struggle()` gained a `force` flag so the two callers keep their different rules, and it now returns the struck faction's id to feed the p.115 "A little visit" trigger. |
| factions-world-compendium | Faction Events table — Compendium pp.114-115 (missing rows and dropped effects) | medium | 38 of 100 faction-event rolls are journal text with no mechanical effect: rolling 43-49 tells the player "Befriending the leadership — Add +1 Story Point" and awards no Story Point; 88-93... | **FIXED, and it was worse than 38 rolls.** The JSON table is byte-exact against pp.114-115 (all 15 rows, all ranges); the WIRING was the problem. Seven rows produced no mechanical effect - **49 of 100 rolls**: *Tensions rising* (15-19) and *Truce* (20-27) had **no handler at all**, and *Tip off* (35-42), *Befriending the leadership* (43-49), *A little visit* (80-87), *We thought we would do you a favor* (88-93) and *Dark secrets* (94-00) each set a flag on the returned dict that the only consumer - PostBattlePhase, which writes a journal line - never read. All seven now apply: Quest Rumors through the canonical `GameStateManager.add_quest_rumor()`, the Story Point through `add_story_points()`, the Enforcer Rival gated on a real two-turn attack window, the rival elimination behind the book's 3+ Loyalty check, and the Dark Secrets quest recorded with both faction ids so `resolve_dark_secrets_quest()` can apply the -1/-1 and +2 Loyalty on completion. Pinned by a test that reads the 15 rows out of the JSON and fails naming any row with no handler. |
| factions-world-compendium | Terrain Generation is an optional rule — Compendium p.9 setup sequence / p.94 | small | A player who does not own the Freelancer's Handbook, or who deliberately left Terrain Generation off, still gets the Compendium's random terrain layout imposed on every battle setup rathe... | **FIXED, carefully - the naive fix here re-creates a worse bug.** `is_terrain_generation_enabled()` had ZERO callers, so the p.94 opt-in a player can see in the wizard and switch off did nothing; the Compendium layout was imposed on every battle. p.94 is explicit: the generators are "supplied for people who PREFER a randomly generated table setup... for players who prefer to have less direct control". The gate is now consulted - but it must NOT return `{}`, which is exactly the 2026-07-02 bug where an earlier gate on the never-defined `TERRAIN_THEMES` flag left EVERY player with no battlefield. Off means "I lay out my own table", not "no battlefield", so `_blank_table_contract()` returns the same SHAPE with 16 labelled but empty sectors, the sanitized table size, the deployment condition and the p.109 Standard Terrain Set guidance - every downstream consumer (PreBattleUI preview, TacticalBattleUI, BattlefieldMapView, post-battle recap) needs no special case. Detection-proven: neutering the gate fails the off-path tests; 40/40 across the seven battlefield suites unchanged. |
| economy-trade-equipment | Consumables are drawn from the Stash and used as a Free Action (Core Rules p.54) | one-line | A Stim-pack, Booster Pills or Combat Serum bought during the Trading phase shows up in the in-battle "💊 Consumable" picker, but choosing it does nothing: use_stash_consumable returns {"us... | **FIXED** — one line, exactly as scoped, and the cause is the owner/cache split. `get_stash_consumables()` (the list the in-battle picker shows) reads the OWNER via `get_ship_stash()`; `use_stash_consumable()` iterated `_equipment_storage`, the cache populated only at campaign load. So any consumable acquired mid-session — bought in the p.125 Purchase step, rolled off the p.133 Consumables subtable, taken as Battlefield Finds — appeared in the picker and returned `{"used": false}` when tapped, silently. Stim-pack is 30% of the Consumables subtable, so it was the likeliest item in the game to fail. Now owner-backed, with the multi-use decrement mirrored onto the cached copy and the campaign-stash removal made explicit (`remove_equipment` no-ops when the item was never cached). |
| economy-trade-equipment | 11. Purchase Items — pay 3 credits for a roll on the Military Weapon, Gear or Gadget Table (Core Rules p.125) | medium | During Trading, 3 credits can return a 1-credit Hand Gun, a Scrap Pistol or a Frag Vest, and the player never gets to pick which of the three tables to roll on. The printed weights are ig... | **FIXED** — the picker was NOT missing (three buttons, Military/Gear/Gadget, have existed all along; that half of the row was stale). What was wrong is that none of the three rolled a table. Each filtered `equipment_database.json` — the STATS catalogue listing every item in the game — by an invented category (`not Pistol/Melee/Grenade`; every gear row plus every attachment; `Utility Device with rarity != Common`) and picked UNIFORMLY. p.125 names the pp.28-29 CREATION tables, so all three now roll them through `StartingEquipmentGenerator.roll_on_weapon_table()` with their printed D100 weights. The p.28 Military table is 8 rows and holds no pistols, which is why 3 credits could return a 1-credit Hand Gun. Also fixed: the 1-credit basics listed `"Handgun"`, which is not an item name anywhere (canonical is `"Hand Gun"`), so the cheapest weapon in the game stashed as a phantom no stats lookup resolved. `test_purchase_items_and_stash` 9 cases. |
| battle-setup | Notable Sights — the item can be acquired by moving into contact with it, and its listed reward is gained | medium | The app tells the player a Person of Interest is 11" from the table centre and that reaching them is worth +1 story point, the player spends a crew member's whole round walking there — an... | FIXED bf3f797c3 + 7de930a2d — all eight reward types applied. Producer is a 'Reached the Notable Sight' check on the results form (asked, because the fight happens on the player's table); Loot-Cache rows raise extra_loot_rolls rather than rolling twice. |
| battle-setup | Deployment Conditions — Slippery ground | one-line | On a Slippery Ground battle (5% of Opportunity/Patron rolls, 10% of Rival) the player reads the condition once on the pre-battle screen and then gets no reminder anywhere in the round loo... | CORRECTED (Aug 6 audit, Unit 1) — **the row named the wrong condition.** Slippery Ground has always had its in-battle reminder: `BattleRoundHUD.gd:581-582` prints "SLIPPERY: All ground movement -1 Speed." during Quick Actions. The condition that genuinely had NO reminder anywhere was **Gloomy** (p.88), including its second clause — a figure that fires can be fired upon at any range — which inverts normal targeting. Gloomy is FIXED 0f10cbfcd (audit F5) at all three phase branches: `BattleRoundHUD.gd:578, 587, 602`. |
| patrons-rivals-quests | "If you just fought a battle that was part of a Quest, roll a D6" — quest progress is gated on the BATTLE being a Quest mission (Core Rules p.120 Step 3) | one-line | Once a Quest exists, EVERY battle advances it — an Opportunity mission, a Patron job, even a forced Rival showdown each roll for quest progress and hand out Quest Rumors on a 4-6. Combine... | FIXED 2c44839f0 |
| patrons-rivals-quests | "If you are not currently on a Quest, roll a D6… If the roll is equal or below the number of Rumors, remove all Rumors from your roster" (Core Rules p.85 Step 5) | small | Rumors are never spent. A crew with 4 Rumors keeps 4 Rumors forever and re-rolls the Quest trigger every single campaign turn (66% chance each turn), each success silently overwriting `pr... | FIXED 2c44839f0 |
| patrons-rivals-quests | "First, you must check that your Rivals give the opportunity to choose your battle!… This will prevent you from doing whatever you had wanted to do this campaign turn" (Core Rules p.85 Step 6) + "Once a Rival has been established, they will always be the same type" (p.92) | medium | When a Rival ambushes you, you fight the Patron job's enemy type on the Patron objective table with the Opportunity/Patron Notable-Sights column — and still collect that job's Danger Pay ... | FIXED (uncommitted) — **an ordering bug: both halves existed and were correct, only their sequence made the rule fail.** p.85 Step 6 opens with the word that IS the mechanic — "**First**, you must check that your Rivals give the opportunity to choose your battle! ... This will prevent you from doing whatever you had wanted to do this campaign turn." `_check_rival_encounter_backend()` ran ~160 lines into `_initiate_battle_sequence`, AFTER the enemy force was generated, the objective rolled off the chosen job's D10 table and the Notable Sight rolled off that job's p.89 column — then merely overstamped `mission_source = "rival"`. A Rival ambush was a Patron job wearing a Rival's name: the Patron's enemy, the Patron objective table, and `PaymentProcessor` still paying that job's Danger Pay for work never carried out. The check is hoisted to the top of the function and `_apply_rival_ambush_override()` clears the displaced job's whole payload (objective_details, victory_condition, placement_rules, notable_sight, danger_pay, benefits, hazards, conditions, patron_id, time_frame, quest_step_id, is_quest_finale). **p.92 shipped with it**: "Once a Rival has been established, they will always be the same type" — `RivalEncounterCheck` now carries `rival_type` (and `is_elite`) off the Rival's own record into `mission_data["enemy_type"]`, which the generator already honours as a preset. A legacy String-shaped Rival yields "", which the generator reads as "roll normally" — the correct answer rather than an invented type. |
| patrons-rivals-quests | "If you just fought against an existing Rival and Held the Field, roll a 1D6… On a 4 or better, they've had enough, and you can remove them from your Rivals list" (Core Rules p.119 Step 1) | small | Hold the Field against a Rival whose figures all Bail on Morale (a common outcome — p.118 morale can rout a force with zero kills) and the removal roll never happens: you cannot shake the... | FIXED (uncommitted) — VERDICT CONFIRMED and **broader than the row recorded**. `fought_existing_rival` was derived by scanning `ctx.defeated_enemies` for an `is_rival` stamp, so it was really asking "did we KILL any of them". That is false in two common situations, not one: the row's p.118 morale rout (Bail removes a figure without killing it), AND **every LOG_ONLY battle** — the DEFAULT tier, where the player tracks casualties on their own table and the app's list is empty. In both, the p.119 removal roll never happened, so a Rival you had comprehensively beaten could never be shaken off. **And it compounded**: execution then fell through to `held_field and not fought_existing_rival` and rolled to gain a NEW Rival, so beating a Rival could hand you a second one for the same fight. Identity now comes from the MISSION — `is_rival_mission` / `rival_id`, both already stamped by `CampaignTurnController` before the battle and derived by `BattleResultNormalizer` — which is the funnel rule (the mission carries its own identity) and holds however the fight ended. The corpse scan is kept as an ADDITIONAL id source for legacy results, and the removal roll now sits under a single `held_field` gate over the collected ids. The p.119 modifiers (+1 Tracked, +1 killed a Unique Individual or Lieutenant per errata v1.06, -1 Persistent) were already correct and are untouched. |
| missions-elites-zones | Black Job victory reward — +1 XP for EVERY crew member, participants or not | small | A crew member in Sick Bay, on a crew task, or otherwise held back during a Black Zone victory gets 0 XP where the book gives them 1. With a 7+ member roster and typically 6 deployed, the ... | FIXED (Aug 6) — the +1 itself was already in `_calculate_crew_xp`, but that runs inside a loop over `ctx.crew_participants`, so **the one clause the book went out of its way to spell out was the one clause that never fired**: p.151 says "even if they did not participate in the battle", and the crew left on the ship got nothing. A second pass now pays absent crew, reusing the same Bot gate (errata v1.06: a Bot cannot earn XP) so a Black Job is not the one battle that pays them. Placed AFTER the "Tough fight" block on purpose — that rule draws a random survivor from `xp_awards`, whose own comment defines that set as surviving non-Bot PARTICIPANTS, so appending absentees first would let it land on someone who never fought. |
| missions-elites-zones | Expanded Missions — Objective overview (two-objective results) | medium | 40% of Expanded Missions rolls (61-100) tell the player "Two objectives, BOTH required" or "Two objectives, complete ONE" and then list a single objective with a single time constraint. T... | **FIXED.** Compendium p.74 rolls 61-85 and 86-100 both read *"Generate two objectives, EACH WITH THEIR OWN TIME CONSTRAINTS"*; p.75 rolls the specific-objective table *"once for each objective"*; p.76 says *"If you have two objectives, a constraint is determined SEPARATELY for each objective."* The overview row has carried `count: 2` since the table was extracted and NOTHING read it, so 40% of Expanded Missions rolls told the player "Two objectives, BOTH required" and then listed exactly one objective with one time limit - a briefing that contradicted itself on the same card. `_enhance_job_with_compendium()` now loops `count` times, rolling an objective AND its own time constraint per iteration into `job["dlc_objectives"]`; the singular keys stay as objective 1 so every existing reader keeps working, and the briefing numbers them `[1]` / `[2]` (a one-objective mission prints no number - there is nothing to distinguish). Duplicates are deliberately NOT rerolled: the book says only "roll once for each objective" and two of the same objective is a legal, playable outcome, so a reroll rule would be invented. |
| missions-elites-zones | Black Job — 'Your Day in Hell' mission type is rolled once and recorded | small | The Black Zone briefing can show 'Destroy strong point' one moment and 'Penetrate the lines' the next if the panel refreshes, and the campaign journal never records which Black Job was at... | **FIXED - two defects meeting in the middle.** `BlackZoneSystem.roll_mission_type()`'s only caller was inside `MissionPrepComponent`'s card BUILDER, which runs on every panel rebuild - so the pp.150-151 D10 'Your Day in Hell' re-rolled each time and the briefing could read "Destroy strong point" one moment and "Penetrate the lines" the next. Meanwhile `PostBattleCompletion.gd:161` reads `battle_result["black_zone_mission"]` to journal which Black Job was attempted and `CampaignJournal.gd:204` renders it: **two live consumers on a key no producer anywhere ever wrote.** New `WorldPhaseController._stamp_black_zone_mission()` rolls once and persists to `campaign.progress_data`, called at BOTH zone-stamp sites in exact symmetry with `_stamp_red_zone_threat` (a guard applied to one of two sites is the shape that produced half this ledger, and a test now pins the call counts equal). `black_zone_mission` added to the normalizer passthrough so it reaches the waiting consumers; `MissionPrepComponent` READS it (mission dict -> campaign -> a not-persisted preview roll); `CampaignPhaseManager` erases it at turn rollover so the next Black Job rolls its own rather than inheriting the last one's. |
| missions-elites-zones | Expanded Missions — briefing headings for the rolled results | one-line | The job-details panel renders "OVERVIEW: ", "SPECIFIC OBJECTIVE: ", "TIME CONSTRAINT: ", "PATRON CONDITION: " and "EXTRACTION: " with nothing after the colon, immediately followed by an i... | FIXED 56714e6c2 — the rows in missions_expanded.json carry `id` and `instruction` and have NO `name` key, so every .get("name", "") was "". The instruction is the book's own self-labelled line; it is printed and the duplicate heading dropped. |
| post-battle | Step 8 — Injury Table roll 31-45, Crippling wound: surgery OR permanent stat loss (p.122) | medium | A Crippling Wound costs the player nothing but time — no 1D6-credit surgery bill and no -1 to Speed or Toughness. The worst survivable injury in the game is mechanically identical to a Se... | FIXED bffe2c8d1 — data/injury_results.json has carried surgery_cost_roll AND the stat_reduction block since it was written and neither had an ACCESSOR. The -1 to the higher of Speed/Toughness now applies and the 1D6 surgery is a pay-to-undo offer in the wizard: the backend resolves injuries before any UI exists to ask, so a choice that waits for a prompt is a choice that never happens. |
| post-battle | Step 12 — Campaign Events 79-81 (Renegotiate Debts), 98-100 (Great Story), 57-59 (New Captain), 82-84 (Rumors of War) (pp.127-128) | medium | Rolling 79-81 while carrying ship debt earns 2 credits instead of wiping 1D6+1 off the loan. Rolling 98-100 after a casualty gives a story point instead of the +1 Luck that character earn... | FIXED — 57-59 New Captain rolled the D6 and discarded it (no flag moved, no 3 XP, no departure), 79-81 Renegotiate Debts paid the debt-free bonus to crews in debt and never touched the loan, 82-84 Rumors of War had no producer for its +2 (now planet-scoped and cleared on travel), 98-100 Great Story never paid the +1 Luck the book puts first. |
| post-battle | Step 13 — Character Events 52-55 (Scars), 24-26 (Good Food), 42-45 (Heart to Heart), 67-68 (True Love) (pp.129) | small | Scars Tell the Story is +2 free XP for an uninjured character who should get nothing. Good Food is +1 free XP for a character in Sick Bay who should instead get a turn back. Heart to Hear... | FIXED — 52-55 Scars paid +2 XP unconditionally (the largest free XP source on the table), 24-26 Good Food paid XP to a character in Sick Bay who should have got a turn back instead and ignored the Engineer exclusion, 42-45 Heart to Heart paid one of the two characters the rule names, 67-68 True Love never paid the Motivation-specific 1D6 XP. |
| post-battle | Step 10 — Advanced Training payment: "The cost can be paid using unspent XP, credits or any combination thereof" (p.124) | medium | The book's own worked example is impossible in the app: a character with 8 XP and 12 credits cannot buy Pilot Training (cost 20). Training is XP-only, so it is unaffordable until very lat... | FIXED c942fec91 |
| turn-upkeep-travel | Repair Your Kit crew task — the item is actually repaired, and Engineer +1 | medium | The task always reports 'Item repaired' and the damaged item stays damaged forever, so a broken weapon is permanently broken. And an Engineer-species character — the one crew type the boo... | FIXED (this commit) |
| turn-upkeep-travel | World Traits Table — the 40 traits' mechanical effects | large | The world you land on is a paragraph of text. Fuel refinery does not make travel cost 3, Fuel shortage does not raise it, Lacks starship facilities does not cap repairs, Bureaucratic mess... | PARTIAL 4fe221f50 + a7449cea6 — WorldTraitEffects.gd is now the SSOT and data/world_traits.json carries a structured `effects` block for all 31 campaign-side traits (the 11 battlefield ones stay with FPCM_BattlefieldGenerator, pinned by a test in both directions). 19 traits have a LIVE consumer: fuel_refinery, fuel_shortage, bureaucratic_mess, travel_restricted, high_cost, technical_knowledge, easy_recruiting, opportunities, corporate_state (patron bonus only), restricted_education, expensive_education, heavily_enforced, rampant_crime, dangerous, vendetta_system, invasion_risk, imminent_invasion + military_outpost (invasion roll only), unity_safe_sector. STILL UNWIRED — the resolver exposes each of these, they need a call site: lacks_starship_facilities (repair credit cap), medical_science, bot_manufacturing, shipyards, adventurous_population, booming_economy, busy_markets, weapon_licensing, import_restrictions, free_trade_zone, interdiction, alien_species_restricted, war_progress_modifier (2 traits), corporate_state forced-type + blacklist  **CLOSED Aug 7** — ELEVEN accessors on the byte-faithful resolver had ZERO callers; each now has one, pinned by `test_every_world_trait_accessor_has_a_live_consumer` so a NEW accessor added without a consumer fails rather than shipping inert. `repair_credit_cap` → `UpkeepPhaseComponent.repair_hull_points()` (credits only — banked repair parts and cashed-in Salvage are not credits, which is what makes the trait survivable; counter reset at rollover). `medical_care_cost` → the Medical Care entry. `bot_upgrade_cost` → `AdvancementSystem.install_bot_upgrade()`, applied at BOTH the affordability check and the charge. `ship_component_cost` → `ShipManager._get_component_cost()`, so the DISPLAYED and CHARGED prices come from one expression. `weapon_purchase_cost` + `selling_forbidden` → `PurchaseItemsComponent` (the Military roll's affordability check now uses the same number it charges; the sell gate lives in the HANDLER, not only the widget). `extra_trade_roll_cost` → the Busy Markets roll (row 157). `rerolls_credit_reward_ones` → `PaymentProcessor`, per DIE inside the loop and BOUNDED, because Compendium p.32 "Money is Tight" has a minimum of 1 that an unbounded reroll could never escape. `war_progress_modifier` → `GalacticWarProcessor`. `forced_patron_type` → patron creation, plus the p.74 Corporate State BLACKLIST, which had no writer at all — `RivalPatronResolver._apply_corporate_blacklist()` records the planet on failure and `CrewTaskComponent._patron_blacklisted()` reads it. `recruit_extra_candidates` → a real CHOICE of candidates ("and then choose who to hire"), never an auto-pick |
| turn-upkeep-travel | Resolve Rumors — remove all Rumors from your roster when a Quest is received | small | Rumors are never spent. Once the player banks 5-6 Rumors the D6 succeeds essentially every campaign turn, and because the quest-active gate is also dead, the game hands out a brand-new Qu... | FIXED 2c44839f0 |
| turn-upkeep-travel | Fleeing an Invasion — you lose everyone you knew on that world, and the shipless/no-credits escape routes | medium | Escaping an invaded world is consequence-free: you keep every Rival and every Patron you built up there, so fleeing is strictly better than staying. And a crew stuck on an invaded world w... | FIXED (Aug 6, final sweep) — new `src/core/campaign/InvasionFlight.gd`. p.69 "Regardless of how you leave, all Rivals, Patrons, and other people known to your crew on this world are lost": ALL Rivals go with no follow roll (the 1D6 / Compendium p.49 Elite 4+ belongs to the p.72 travel steps a flight does not perform), all Patrons except those marked Persistent (documented reading at the site — the clause is scoped to people "on this world", and p.151 grants Patrons that are explicitly "Persistent across worlds"), and their orphaned job offers are dropped. Both escape routes exist: sell gear at 1 credit per TWO items, then evacuation passage (lose all credits + 1D6 items from Stash **and** character sheets, ship abandoned via `ShiplessSystem.apply_ship_destruction`). The Travel button now enables while fleeing instead of stranding a broke crew — on an ordinary turn it stays disabled, which IS p.69's "you are stuck until you do". Applies on both departure routes: the successful 2D6 8+, and the later trip after surviving a forced Invasion Battle (carried as `progress_data["invasion_flight_pending"]`) |
| turn-upkeep-travel | Sick Bay exit — recovered characters cannot perform a task that campaign turn | small | A character whose last Sick Bay turn ticks off at rollover walks straight into the Crew Tasks screen and takes a full task the same turn — an extra Explore/Trade/Patron roll per recovery ... | FIXED 5927ecbe3 — p.76 makes leaving Sick Bay a TWO-step release and only the first existed. `recovered_this_turn` is stamped at the rollover release and read by CrewTaskComponent, cleared at the top of the next rollover so it lasts exactly one turn. |
| turn-upkeep-travel | Train task resolves a Character Upgrade immediately; Find a Patron offers an EXISTING Patron | medium | Training banks XP that sits unspent until the player happens to open the post-battle advancement screen, and the p.77 'resolve that immediately' beat never happens. And the Patron pool in... | FIXED (Aug 6, final sweep) — Train now resolves the upgrade where p.78 says: "If this means they may make a Character Upgrade ... resolve that immediately." `CrewTaskComponent._offer_immediate_upgrade()` opens `CharacterUpgradeDialog` on the LIVE crew entry, and only when an upgrade is genuinely affordable (a single Train earns 1 XP against a cheapest p.123 cost of 5, so most turns cross no threshold and a dialog every turn would be worse than the bug). A picker, never `auto_advance_character()` — that takes "first available" and would spend a character's Luck savings on Speed. The Find-a-Patron half was closed with row 205 (`_grant_patron_job_offers` draws on an EXISTING Patron and mints a new one only for the second of two offers, p.77); pinned here so it cannot regress |
| turn-upkeep-travel | Repeat Patron keeps the same Benefit; up to two characters on any one task | small | A loyal Patron you have worked for five times rolls a different Benefit (or none) every single job, so building a relationship with one employer has no payoff. And only one crew member ca... | FIXED 878057d6a (Benefit remembered per Patron; the two-per-task half was already right except a fabricated max_crew 1 on repair_kit) |
| battle-resolution | Saving Throws — Multiple Saving Throws (p.46) and innate Bot/Soulless armor plating | small | A Bot or Soulless crew member with no purchased armor gets NO saving throw in any auto-resolved battle, instead of the 6+ the book guarantees (5+ for Assault Bots) — they die roughly 17% ... | FIXED 4b7102dd0 — was min() ('best wins'), losing the -1 the book grants for stacking. Both printed examples now pass. Innate plating now covers all four species, not just Soulless |
| battle-resolution | Weapon Ratings — Shots (p.49): the number of attack dice you roll | small | On the standard (no-DLC) campaign, an Auto Rifle (2 shots), a Rattle Gun (3 shots) and a Hyper Blaster (3 shots) all fire exactly once per round in auto-resolve — identical to a 1-shot Co... | FIXED f6e85b238 — auto-resolve called resolve_ranged_attack() ONCE per attacker and never read Shots, so Rattle gun (3), Hyper blaster (3), Auto rifle/Shotgun/Plasma rifle/Needle rifle/Machine pistol/Shell gun/Hand flamer/Flak gun/Cling fire pistol (2 each) all fired a single shot. Now loops over Shots, same target (required by Focused, p.51), breaking early once the target is down |
| battle-resolution | Impact trait (p.51) — a second Stun marker on an already-Stunned target | small | The Suppression maul — the game's dedicated Impact weapon — is mechanically identical to any other Damage +1 melee weapon. Its ability to stack a Stunned enemy toward the 3-marker knockou... | FIXED b776e8726 — was unconditional causes_stun, so Impact stunned healthy targets; it only adds a SECOND marker to an already-Stunned figure |
| battle-resolution | Stunned — Move OR Combat Action, not both (p.40); marker removed after acting | small | In auto-resolved battles a Stunned figure — crew or enemy — suffers no penalty whatsoever: it moves and fires exactly like an unstunned one, and its Stun evaporates at the end of the roun... | FIXED f6e85b238 — stun was a BOOLEAN in auto-resolve, so markers could never accumulate and the p.40 '3 or more = knocked out and removed from play' could not fire at all. Markers now accumulate per hit and trigger removal, credited as a kill |


---

## Compendium chapter trace — Aug 3 2026 (NEW findings, not part of the 143)

Full walk in **`docs/COMPENDIUM_CHAPTER_TRACE_2026-08.md`** — all 30 chapters of the
Compendium TOC (pp.4-5), producer → key → consumer, verified by hand.

**17 LIVE / 3 PARTIAL / 10 DEAD.** The ten dead chapters have book-exact data and a
correctly flag-gated API; what they lack is a caller — or, twice, a caller whose
argument is permanently false.

⚠ **Three of the twenty original LIVE verdicts were wrong**, all found by checking
the trace against this ledger rather than trusting it. Treat the remaining LIVE rows
as ~90% confidence, not fact.

| Chapter | Dead because | Flag |
|---|---|---|
| Player vs Player pp.35-38 | 4 gated funcs, zero callers repo-wide | PVP_BATTLES |
| Expanded Co-op pp.39-41 | 2 gated funcs, zero callers | COOP_BATTLES |
| AI Variations p.42 | `roll_ai_behavior` / `get_ai_behavior`, zero callers | AI_VARIATIONS |
| Enemy Deployment Variables p.44 | `deployment_variables.json`, zero loaders | DEPLOYMENT_VARIABLES |
| Elite-level Enemies pp.48-65 | `elite_enemy_types.json` never even LOADED; getter commented out at `DataManager.gd:745` | ELITE_ENEMIES |
| Expanded Quests p.78 | `roll_quest_progression` / `get_quest_conclusion`, zero callers | EXPANDED_QUESTS |
| Expanded Connections p.80 | 3 gated funcs zero callers; `CharacterConnections.gd` referenced by nothing | EXPANDED_CONNECTIONS |
| Grid-based Movement pp.90-93 | `TacticalBattleUI:6123` reads `grid_movement_instructions`, zero producers | GRID_BASED_MOVEMENT |
| **Difficulty Toggles pp.32-34** | none of the 12 toggle ids is read anywhere in `src/` — confirms the pre-existing ledger row | DIFFICULTY_TOGGLES |
| **Fringe World Strife pp.148-151** | gated on `is_fringe_world`, which no producer writes; and p.148 is a 1D6-on-arrival plus a per-world Instability accumulator, not a boolean — the implemented mechanism is the wrong one | FRINGE_WORLD_STRIFE |

**The last two rows were published LIVE in the first version of the trace and were
wrong.** The ledger already had both as OPEN; trace and ledger disagreed and the
ledger won. The error was stopping at the caller: `should_check_strife()` really is
called, with an argument nobody writes; and four resolvers really do preload
`compendium_difficulty_toggles.gd`, to call the Dramatic Combat and Casualty
functions that share that file. **Trace to the value, not to the call site.**

Three were listed here as PARTIAL. **All three are STALE — re-verified against
the code Aug 7 and every one has a live caller:** `apply_dramatic_weapon_profile()`
is called from `BattleResolver.gd:113` (it replaces the printed profile of any
weapon the p.88-89 table lists); `roll_detailed_injury()` is called from
`InjuryProcessor.gd:351` — the PLAYED post-battle path, before the Core Rules
D100, not just the auto-resolve branch; and the Loans step-2 constant is gone,
replaced by `base_cost + surcharge` off `_ship_cost_from_core_rules_p31()` and
`CompendiumWorldOptions.loan_origin_surcharge()`, so a Unity loan and a
Suspicious Character loan no longer cost the same.

| Chapter | What lands | What was listed as missing (now STALE) |
|---|---|---|
| Dramatic Combat pp.87-89 | Adjusted Shooting in all 4 resolvers + rule text | Dramatic Weapons stat table pp.88-89 — `get_dramatic_weapon_stats()` zero callers |
| Casualty Tables p.99 + Detailed Injuries p.101 | both roll, from `TacticalBattleUI:5488/:5496` | that block is the **auto-resolve** branch. `InjuryProcessor` — the PLAYED post-battle path — never references them, so the primary path for a tabletop companion falls back to the Core Rules D6 death check |
| Loans pp.152-156 | Steps 1/3/4 roll correctly | **Step 2 is `var loan_amount: int = 20`** (`TradePhasePanel.gd:835`). Book: base = ship cost p.31, **+5cr Unity**, **+1D6cr Free Trader / Suspicious**. A Unity loan and a Suspicious Character loan cost the same |

**Gating is not wiring, and the two are measured differently.** All 33 `ContentFlag`s
except `DEPLOYMENT_VARIABLES` and `ELITE_ENEMIES` are read somewhere — yet six more
chapters are dead, because the flag is read *inside the getter nobody calls*. A flag
census will never find those six. Trace the consumer.

Fixed in the same pass (`DLCContentCatalog.UNIMPLEMENTED_FLAGS` 2 → 8) so a player who
buys Freelancer's Handbook no longer sees six switches that change nothing. Also
corrected six wrong page citations, one of them player-visible in the creation wizard
(Expanded Factions was cited as pp.148-153, which is Fringe World Strife's range).

---

## ⛔ Sourcing trap found Aug 2: the Compendium contains a SECOND, CONFLICTING weapon table

**Before "correcting" any weapon value, check which SECTION of the Compendium you found it in.**

`docs/compendium.md` (~lines 5655-5714) prints a complete weapon table whose values contradict
the Core Rules table (`docs/core_rules.md` ~4020-4066):

| Weapon | **Core Rules (governs)** | Compendium *Game Options* |
|---|---|---|
| Suppression maul | Brawl, **1** dmg, *Melee, Impact* | Brawl, **2** dmg, *Melee, **Stun*** |
| Ripper sword | Brawl, **1**, *Melee* | Brawl, **2**, *Melee* |
| Shotgun | **12"**, **2** shots, 1, *Focused* | **8"**, **1** shot, 1, ***Critical*** |
| Shell gun | **30"**, **2**, 0, *Heavy, Area* | **18"**, **—**, 0, *Area* |
| Needle rifle | 18", **2**, 0, *Critical* | 18", **1**, 0, *Critical, **Piercing*** |
| Shatter axe | Brawl, **2**, *Melee* | Brawl, **3**, *Melee, Clumsy, **Shockwave*** |
| Marksman's rifle | 36", 1, 0, ***Heavy*** | 36", 1, 0, ***Critical*** |
| Scrap pistol | **9"**, 1, 0, *Pistol* | **7"**, 1, 0, *Pistol* |

That table sits under a `## Game Options` heading, followed by `## Designer notes`:

> "While many weapons have seen **minor tweaks**, of particular note are pistols... and melee
> weapons, which have received small boosts across the board. Area weapons no longer have
> inherent Shots on their profile, instead dealing damage only through the Area rule..."

**"Have seen tweaks" + placement under Game Options = an opt-in alternative weapon set, NOT
errata.** The Core Rules table governs standard 5PFH; `data/equipment_database.json` must match
it. Traits that exist only in the option set (**Overheat, Shockwave, Shrapnel, Burn**) must not
be attached to Core Rules weapons. If the alternative set is ever implemented it belongs behind
a toggle alongside the other Compendium Game Options, never as the default.

**A grep proving a value "is in the Compendium" is NOT sufficient sourcing.**

### This also means the audit quoted the wrong Area trait

- **Core Rules p.51 (implement this):** "Resolve all shots against the initial target. **They
  cannot be spread.** Then resolve **one bonus shot against every figure within 2"**."
- **Compendium *Game Options* (do NOT implement as default):** "Select a target point within
  range. Every figure within 2" of the target point are hit on an unmodified D6 roll of **4+**
  (**5+** if partially obscured from the blast)."

The Area finding in this document describes the Compendium version.

### Related discrepancy, unresolved

`data/elite_enemy_types.json:374` and `data/RulesReference/EliteEnemies.json:247` both state
**"Cop killer: Enforcers always become Rivals after a battle"**, while the Core Rules
(and `data/enemy_types.json:39`) state **"Cop killer: As Rivals, +2 to their numbers"**. These
are different rules. Likely the Elite-Enemies chapter legitimately restates it, but it has not
been verified against the Compendium page — check before touching either file.

## Verbatim book evidence for the battle-resolution domain

Extracted Aug 2 to save re-extraction; see the To Hit and Suppression rows above for the
headline results. Full brief (Stunned p.40, Brawling p.45, Aiming/Panic Fire p.46, all weapon
traits p.51, all six consumables p.54) was written during the session and its conclusions are
folded into the rows in this table.

---

## Handoff — state as of Aug 2 2026, end of the battle-resolution pass

> **HISTORICAL.** Superseded Aug 7 2026 — the ledger closed at 0 open / 0 partial /
> 136 fixed / 1 corrected. Kept for the route it describes, not as a worklist.

**64 open / 79 resolved-or-partial of 143.** Branch `campaign-editor-and-fixits`.
Route and phasing: `docs/RULES_WIRING_CLOSEOUT_PLAN.md`. The short version — 40 of the
73 are DLC or endgame content no tablet tester can reach, so the near-term target is the
~33-row core loop, of which 6 are blocked on the Story Track session's files.

### Domain standings

| Domain | Open | Note |
|---|---|---|
| battle-resolution | **1** | only "dead duplicate calculators" — cleanup, no rules impact |
| turn-upkeep-travel | 7 | World Traits (40 effects) is the big one |
| battle-setup | 8 | deployment conditions, seize initiative |
| patrons-rivals-quests | 12 | Time Frame never expires; BHC subtables; Patron travel expiry |
| post-battle | 13 | Advanced Training, Crippling Wound, injury equipment loss |
| economy-trade-equipment | 15 | Trade Table 24 no-award rolls, gun mods, on-board items |
| factions-world-compendium | 20 | DLC-gated; a tester without the DLC never sees any of it |
| missions-elites-zones | 20 | Black Jobs, Red Zone conditions, Elite enemies |

### If the goal is a TABLET TEST SESSION, do these first

Ranked by "will a tester actually hit this in one sitting", not by finding count.

~~1. patrons-rivals-quests — Quest is unplayable.~~ **DONE `2c44839f0`** —
   "Continue a Quest" job option, p.89 objective column reachable, finale forced
   to Fight Off with +1 fight-to-the-death enemy, `is_quest_finale` produced so
   its four consumers fire, Rumors actually spent, progress gated on Quest
   battles, p.64 victory counts Quests. `tests/unit/test_quest_chain.gd`.
   The p.90 Defend objective landed alongside it in `3aa8d0b88`.

~~2. battle-setup — Number of Opponents difficulty modifiers.~~ **DONE
   `24c657af4`** — the count was reading `danger_level` (the p.83 Patron Danger
   Pay rating) as if it were the difficulty mode. Also fixed: Insanity's missing
   +1, the p.93 short-crew -1, and the p.99 Interested Parties reroll being
   applied to every Quest battle. Red Jobs untouched; zone tests still 7/7.

**Next up, in order:**

~~3. economy — the Trade Table's 24 no-award rolls.~~ **DONE `e2e56b985`** —
   the six "roll on another table" entries now award; the p.28 Low-Tech Weapon
   and p.29 Gear tables were also being resolved off the LOOT table.

~~5. post-battle — Advanced Training.~~ **DONE `c942fec91`** — nothing was
   spent and nothing was learned; plus XP-only payment, the unenforced
   one-course and one-attempt rules, Bot credits-only, Broker +1, the Feral and
   Engineer cost modifiers, and a fabricated eighth course. It was ALSO
   unreachable on a loaded save (`Array[Resource]` filter vs Dictionary crew).

**Next up:**

~~4. turn-upkeep-travel — World Traits.~~ **MOSTLY DONE `4fe221f50` +
   `a7449cea6`** — `WorldTraitEffects.gd` is the SSOT, all 31 campaign-side
   traits carry structured `effects` in JSON, and 19 have a live consumer
   (travel cost, departure, upkeep, repair, recruiting, patron search, Advanced
   Training, enemy numbers, Rival conversion, Invasion). The remaining 12 need a
   CALL SITE only — the resolver already exposes each one, and the tracker row
   lists them by name. Pick them off wherever you next touch the owning screen;
   `test_world_trait_effects.gd` fails if a new trait ships without an effects
   block, so the data side cannot regress.

~~6. patrons-rivals-quests — Patron Time Frame + the BHC subtables.~~
   **DONE `fab705684` + `592a67212` + `cfbd4f91f`.** `PatronJobEffects.gd` is the
   SSOT; `data/patron_generation.json` gained a stable `id` and an `effects` block
   per row, and JobOfferComponent's SECOND hardcoded copy of all 21 rows is gone.
   Offers now persist on the campaign with a real `deadline_turn`, lapse when it
   passes (Vengeful fires on a lapse), and leave with a Patron who does not follow
   you. 19 of 21 rows are applied.

   **Two rows still only reported, not applied** — pick these up here:
   - **Private Transport** (p.84, "If you have Rivals, they cannot track you this
     campaign turn") needs the p.85 Check for Rivals roll in
     `RivalEncounterCheck.gd`, which held another session's uncommitted work.
     `PatronJobEffects.blocks_rival_tracking(mission)` is ready; it needs one
     early-return.
   - **Busy** (p.84, "If the mission is a success, the Patron offers a new job
     next campaign turn"). `offers_new_job_on_success()` exists with no caller —
     and wiring it as written would be a **no-op**, which is the interesting
     part. Completing a job removes that offer from the store, so next turn the
     Patron has no live offer and generates one anyway: every Patron already
     offers work every single turn. Busy is only worth a table slot in a game
     where they do NOT, so the real divergence is our offer CADENCE, not the
     missing flag. Do not wire a distinction that does not exist; decide the
     cadence question first (the book never states a per-turn offer rate, which
     is why this needs a judgement call rather than a page citation).

~~7. patrons-rivals-quests — the three p.99 enemy-trait rules.~~
   **DONE `5eeee2c39`.** `EnemyTraitRules.gd` matches on the trait NAME before
   the colon, so the rest of the encounter tables' `special_rules` strings are
   now cheap to wire the same way — and `2d052d2de` + `1ac3697ab` did seven
   more: Cop killer, Scavengers, Tough fight, Careless, Alert,
   Prediction/Unpredictable and Going medieval.

   **What is left is print-only ON PURPOSE, probably.** Every remaining trait is
   an IN-BATTLE rule — Cowardly ("Lieutenants are affected by Morale dice"),
   Dogged (Fearless at 1-2 figures), Ferocious (+1 Brawling when initiating),
   Stubborn, Bad shots, Trick shot, Aggro, Easy targets, Needle fangs, Quick
   feet, Leap, Shimmer, Pack hunters, Up close. This is a tabletop COMPANION:
   the player resolves combat on the table, so surfacing these in the briefing
   may be the whole job. Decide that deliberately rather than by omission.

   **The sharpest lesson of the group** came from Going medieval: replacing a
   loadout ONCE is not enough. The Lieutenant branch re-rolls the basic column
   (p.93, "Lieutenants will both roll separately for their weapons") and so does
   Varied Armaments, so the resolver was correct and two later lines quietly
   undid it — the war leader kept a handgun while his mooks held blades. Test at
   the GENERATOR, not just the resolver, whenever a rule REPLACES rather than
   adjusts.

8. **patrons-rivals-quests — the two BHC rows left over** (Private Transport,
   Busy) — see the detail under item 6.

~~9. post-battle — the Injury Table's consequences beyond Sick Bay turns.~~
   **DONE `bffe2c8d1` + `d945a3ecb` + `b14974c37`.** 35% of every roll on the
   table produced nothing but time. Equipment: the flags were computed, stored
   on the injury dict and read by NOTHING, so p.78 Repair Your Kit had nothing
   to repair in any campaign. Crippling wound: the JSON had both halves of
   "1D6 credits of surgery OR -1 permanent" and neither had an accessor.

   **The knock-on chain is the lesson.** Fixing the producer surfaced two more
   layers that were also only half-built, and each was invisible until the one
   above it worked:
   - Campaign Event 45-48 damages the **STASH** (p.127), not carried gear —
     which needs `damaged: true` on the item dict, a flag Assign Equipment
     ("[DAMAGED]" suffix) and Purchase Items (sell-list exclusion) have always
     read and nothing had ever written. So p.122's "cannot be used until
     Repaired" was ALREADY enforced for stash items, with nothing to enforce on.
   - …which meant Repair Your Kit could not reach them, because it scanned only
     the acting character's status_effects. p.78 draws no such line.
   - Sick Bay reductions (Friendly Doc p.126, Health Insurance p.84) wrote
     `injury_recovery_turns`, a legacy mirror the turn rollover ignores.

   **Next in this area:** Step 8's other unimplemented row is nothing — the
   table is now complete. The nearest neighbours still open are Step 9's
   "Ability Increase Table is spent, not rolled" (the wizard says "Roll for
   advancement" and prints results that change nothing) and Step 10's Medical
   school / Bot technician injury rerolls, which now have a real Injury Table
   to reroll against.

Deliberately deprioritised for a tablet test: **factions** (DLC-gated, invisible
without the expansion) and **Elite enemies** (endgame).

### Hard-won rules that cost time to learn — read before editing

- **The Compendium prints a SECOND weapon table** under `## Game Options` whose
  values contradict the Core Rules (Shotgun 8"/1 shot vs 12"/2; Suppression maul
  2 damage Melee+Stun vs 1 damage Melee+Impact). Its designer notes call them
  "minor tweaks". It is an OPT-IN VARIANT, NOT ERRATA. A grep proving a value
  "is in the Compendium" is NOT sufficient sourcing — check the section.
  Same split applies to the **Area** trait.
- **gdUnit4 reports a parse error as "No test cases found" and EXITS 0.** A green
  exit code proves nothing. Check the executed-case count, every time.
- **A `.get(key, fallback)` whose fallback is a plausible NEIGHBOUR is worse than
  a crash.** Two p.88/p.94 tables picked the wrong column for years because
  `tables.get(mission_source, tables.get("patron", {}))` quietly turned every
  Rival battle into a Patron one — a quarter of them fought wildlife the book
  excludes outright. The output looked entirely reasonable at every step. When a
  key is a COLUMN SELECTOR, map the vocabulary explicitly and `push_warning` on
  anything unrecognised; never let an unknown source impersonate a known one.
- **When a rule says "consult the appropriate column", pin the columns with a
  TILING test.** Every column must cover 1-100 (or 1-10) with no gap and no
  overlap: a gap means a legal roll silently yields nothing, which is
  indistinguishable from the table's own "No Condition"/"no effect" row and
  therefore hides forever.
- **`verify_scripts_parse.gd` prints `=== N checked, 0 definite failures ===`
  with real parse errors in the same output.** Grep the run for `Parse Error`
  separately — the summary line is not the verdict. It caught a genuine one this
  session (`EquipmentTransferService.new()` takes a campaign) that the tally
  reported as clean. The tool ALSO emits `Compile Error: Identifier not found:
  TweenFX / GameStateManager / DataManager` for anything touching an autoload,
  because `--script` mode has no autoloads. Those are noise; `Parse Error` is not.
- **A correct JSON table is not a live one.** `patron_generation.json` held all 21
  BHC rows byte-correct against pp.83-84 — and `JobOfferComponent` carried a
  SECOND hardcoded copy in GDScript, so the JSON was decorative and a correction
  to it would have changed nothing in play. Before "fixing" a data file, grep for
  a second implementation of the same table.
- **A live consumer with no producer looks exactly like a working feature.**
  `NewWorldArrival.is_persistent_patron()` read `is_persistent` off a Patron and
  nothing anywhere ever wrote it, so the p.84 Persistent Benefit could not spare
  a single Patron from the travel purge. The travel step LOOKED implemented, and
  was, in one direction only. When wiring a rule, check both halves meet.
- **Godot's JSON parser returns EVERY number as a FLOAT.** `value is int` is
  always false on loaded data; `[1.0, 15.0] != [1, 15]`. `int()` both sides.
- **A test suite can pin fabricated rules as firmly as correct ones.** Four tests
  failed when the to-hit math was corrected, and all four were asserting invented
  behaviour. Convert them, do not delete them.
- **Dead + wrong is a much stronger deletion warrant than dead alone.** CLAUDE.md
  records a sweep that deleted three methods as "zero-caller" when they were only
  zero-caller because their consumer had been removed weeks earlier. Before
  deleting a zero-caller provider, ask what used to call it — but if the content
  also contradicts the book, the question is moot.
- **Fixing a producer is how you find the next missing producer.** Wiring the
  p.122 equipment rows exposed, in order: the wrong container (p.127 says Stash,
  not carried gear), a stash-damage flag with TWO readers and no writer, a repair
  task that could not see the container the book puts items in, and a Sick Bay
  reducer writing a field the countdown ignores. None of those were visible while
  the first producer was dead, because a pipeline with a broken source looks
  identical to one with no consumers. Expect a chain, and re-verify the CONTAINER
  the book names before assuming the target.
- **Your own test catching your own wrong target is the system working.** The
  first Equipment Malfunction test asserted crew gear and failed once the book
  was read properly. Write the assertion against the book's words, not against
  the implementation you just wrote, and it will tell you when you are wrong.
- **A fabricated CITATION is worse than an uncited invention.** `// Ferals ignore
  suppression (Five Parsecs p.20)` attached a real page number to a rule that
  page does not contain, and survived review by looking sourced.

### Verification commands that work

```powershell
# Parse sweep — the fastest global check. "0 definite failures" is the verdict;
# the '?' markers are its indeterminate bucket (it flags even itself).
& "$GODOT" --headless --path "$ROOT" --script tests/tools/verify_scripts_parse.gd

# gdUnit4 — note -c, and NEVER --headless. Always read the case COUNT.
& "$GODOT" --path "$ROOT" --script addons/gdUnit4/bin/GdUnitCmdTool.gd -c `
  -a tests/unit/test_battle_calculations.gd --quit-after 300
```

Battle regression set, all green at handoff (130/130): `test_battle_calculations`,
`test_battle_resolver_router`, `test_battle_funnel_routing`,
`test_zone_job_opposition`, `test_enemy_deploy_markers`,
`test_weapon_table_source_of_truth`.

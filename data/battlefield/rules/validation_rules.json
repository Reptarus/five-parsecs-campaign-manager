{
  "rulesets": {
    "standard_tactical": [
      {
        "rule": "check_firing_lanes",
        "description": "Ensure no firing lane is longer than 15 inches.",
        "params": { "max_length": 15 },
        "fix": { "action": "place_blocker", "params": { "feature_tag": "generic_cover" } }
      },
      {
        "rule": "check_objective_accessibility",
        "description": "Ensure all objectives are reachable from deployment zones.",
        "params": { "max_path_cost": 20 }
      }
    ],
    "urban_specific": [
      {
        "rule": "check_min_feature_density",
        "description": "Ensure urban maps have at least 30% of the area covered by features.",
        "params": { "min_percentage": 0.3, "feature_tag": "building_urban" }
      }
    ],
    "wasteland_specific": [
       {
        "rule": "check_max_feature_density",
        "description": "Ensure wastelands are not too cluttered.",
        "params": { "max_percentage": 0.25 }
      }
    ]
  }
}
